## Review

**Verdict:** NOT-READY
**Reviewer:** aidlc-architecture-reviewer-agent
**Date:** 2026-09-21T03:25:12Z
**Iteration:** 1

### Findings

| ID | Severity | Location | Finding | Required action | Status |
|---|---|---|---|---|---|
| R-01 | Major | backend/src/main/java/com/mastersmith/dataio/csv/CsvRowValidator.java > validateRow(主キー存在確認の呼び出し) と backend/src/main/java/com/mastersmith/dataio/service/CsvImportService.java > rowExists / convertForLookup / readAndValidateRows の catch | 主キー列がINTEGER/DECIMALで、CSVの主キー値が数値に変換できない場合(例: `abc`)、`validateColumn`が`typeMismatch`を記録した後でも`validateRow`は`primaryKeyExists.test(rawPrimaryKey)`を呼ぶ。`rowExists`は`convertForLookup`で`Long.parseLong("abc")`を実行して`NumberFormatException`を投げ、`readAndValidateRows`の`catch (IOException \| RuntimeException)`が受けて`CsvFormatException`(ファイル全体の形式エラー)に変換する。BR8.5/BR8.6は「型変換エラーは行単位のバリデーションエラー(行番号・フィールド単位)」と定めているが、この経路ではファイル全体が形式エラーとなり、他の行のエラーも収集されない。同じcatchは`rowExists`のDBエラー(接続断・SQL誤り)も「CSV形式エラー」に誤分類する。`CsvRowValidatorTest`は`Predicate`をラムダで差し替えているためこの経路を通らず、`CsvImportServiceTest`(3件)にも主キー不正値のケースがない。 | (1) 主キー列自体の型変換/validationRuleが失敗した行では`primaryKeyExists`を呼ばず、行単位エラーのみ返す(または`convertForLookup`の失敗を行エラーに変換する)。(2) `catch`はパース由来の例外(`IOException`・`CSVParser`のIllegalArgumentException)に限定し、DBアクセス由来の例外を`CsvFormatException`へ変換しない。(3) 主キー不正値(数値列に`abc`、桁あふれ)を含むCSVで、行単位エラーが返り全行分のエラーが収集されることを`CsvImportServiceTest`に追加して確認する。 | New |
| R-02 | Major | backend/src/main/java/com/mastersmith/dataio/service/CsvExportService.java > writeRows(`resultSet.getObject` → `printRecord`)と backend/src/main/java/com/mastersmith/dataio/csv/CsvRowValidator.java > convert(DATE/DATETIME) | エクスポートは`ResultSet.getObject`の戻り値をそのまま`printRecord`へ渡す(値の`toString()`)。DATETIME列は`java.sql.Timestamp.toString()`(`2026-01-01 10:00:00.0`形式)になり得るが、インポートは`LocalDateTime.parse`(ISO-8601、`T`区切り)しか受け付けず、`typeMismatch`になる。BR8.1は「エクスポート・インポート双方で同一形式を用いる」と定めているが、エクスポート→編集→インポートの往復が日時列で成立しない(PostgreSQL/MariaDBのドライバは`Timestamp`を返す)。`CsvExportServiceTest`はTEXTとDECIMALの2列(H2)のみで、DATE/DATETIME/真偽値/NULLの出力形式を検証していない。 | エクスポート時にeditorType(または`ResultSetMetaData`の型)に応じて、日付・日時・真偽値をインポートが受け付けるISO-8601等の正規形式へ明示的に整形する。DATE・DATETIME・SWITCH/CHECKBOX・NULLの各列を含むテーブルで、エクスポートしたCSVをそのままインポートして成功する往復テストを追加する。 | New |
| R-03 | Major | backend/src/main/java/com/mastersmith/dataio/service/CsvExportService.java > writeRows(`setFetchSize(FETCH_SIZE)`)、backend/src/main/java/com/mastersmith/dataio/config/BusinessDataSourceConfig.java、backend/src/main/resources/application.yml > mastersmith.business-datasource | BR8.10・performance-design.mdはカーソル+`fetchSize`によるストリーミングを要求する。しかしコードは`Connection`の`autoCommit`を変更せず(`grep -i autocommit`は0件)、業務データ用DataSourceは既定のautoCommit=trueである。PostgreSQL JDBCは自動コミット有効時に`fetchSize`を無視して全件を先読みし、MySQL/MariaDBも`useCursorFetch=true`等の指定がなければ全件をメモリへ読み込む。対象RDBMSとして確定しているPostgreSQL/MySQL/MariaDBの全てで、10万行のエクスポートがアプリケーションメモリへ全件展開される可能性がある。テストはH2のみで、この性質を検証していない。 | エクスポート用のコネクションで`setAutoCommit(false)`(読み取り専用トランザクション)を行い、MySQL系は`useCursorFetch=true`(またはストリーミング用の`fetchSize`指定)を接続URL設定の要件として`application.yml`・ドキュメントに明記する。ドライバ別の前提を`code-summary.md`・`unit-test-instructions.md`に記載し、少なくともPostgreSQLでストリーミング動作を検証する手順(Testcontainers等)を用意する。それができない場合は、BR8.10の未達をリスクとして明示し承認を得る。 | New |
| R-04 | Major | backend/src/main/java/com/mastersmith/dataio/csv/CsvRowValidator.java > validateColumn(空値・欠落列を`convertedValues.put(name, null)`)と backend/src/main/java/com/mastersmith/dataio/service/CsvImportService.java > executeUpdate、backend/src/main/java/com/mastersmith/dataio/csv/CsvColumnDefinitionResolver.java > resolveForImport | インポートはCSVのヘッダーに存在するかを確認せず、対象テーブルの全ColumnConfig(hidden含む)を処理対象にする。UPDATE行は、必須でない列のセルが空、またはその列がCSVに存在しないと、当該列を`NULL`で上書きする(`executeUpdate`は非主キー全列をSETする)。一方エクスポートはhidden列と権限のない列を出力しない(BR8.2)ため、エクスポートしたCSVを編集して再インポートすると、hidden列・権限外の列が全UPDATE行でNULLに破壊される。当該列に`required`があれば逆に全行が`required`エラーになりインポートが成立しない。これは黙示のデータ消失であり、エクスポートとインポートを対で使う主要なユースケースを壊す。この挙動は`rules.md`・`functional-spec.md`のいずれにも仕様として定められておらず、テストもない。 | 仕様として次のいずれかを確定して実装・テストする。(a)UPDATE時はCSVヘッダーに存在する列のみをSETする(存在しない列は既存値を維持し、`required`もヘッダー存在列のみに適用する)。(b)ヘッダーに必要列が不足している場合はファイル形式エラーとして扱う。空セルをNULLとして扱う範囲もあわせて明文化し、機能設計(functional-spec/rules)へ追補する。エクスポート→インポートの往復でhidden列の値が変わらないことを確認するテストを追加する。 | New |
| R-05 | Minor | backend/src/main/java/com/mastersmith/dataio/service/CsvExportService.java > exportCsv(`try (CSVPrinter printer ...)`)と backend/src/main/java/com/mastersmith/dataio/DataImportExportApi.java > exportCsvのJavadoc | `DataImportExportApi.exportCsv`は「`out`は呼び出し元が用意する出力ストリーム。本メソッドはクローズしない」と定めているが、実装は`CSVPrinter`をtry-with-resourcesで閉じるため、`CSVPrinter.close()`が包んでいる`Writer`経由で呼び出し元の`out`(HTTPレスポンスの出力ストリーム等)をクローズする。契約とコードが矛盾し、テストは`ByteArrayOutputStream`(クローズしても影響が出ない)しか使わないので検出されない。 | `CSVPrinter`をtry-with-resourcesにせず`flush()`のみ行う(または`out`をクローズしないラッパーを挟む)ようにするか、Javadocの契約を「クローズする」へ改める。どちらを採るかを決めてテスト(クローズ状態の検証用`OutputStream`)で担保する。 | New |
| R-06 | Minor | backend/src/main/java/com/mastersmith/dataio/service/CsvImportService.java > readAndValidateRows(`rowExists`)、commitRows、executeInsert/executeUpdate | (1)UPDATE行ごとに`SELECT 1`を1回実行する(最大10万行でN+1、NFR1.2の2分目標に対する測定・テストがない)。(2)存在確認はコミットのトランザクション外で行うため、検証後〜コミット前に対象行が削除されると`UPDATE`が0件更新でも`successCount`に数えられる(更新件数を確認していない)。(3)コミット時のDB制約違反(CSV内の主キー重複、一意制約など`RowValidator`が検出できない違反)は生の例外がそのまま呼び出し元へ伝播し、ロールバックはされるが行単位のエラーも`ImportExecutedEvent`(BR8.9は「成功・全体ロールバックのいずれの場合も」発行)も生成されない。(4)テーブル名・列名は引用符なしで連結されるため、予約語の列名(`order`、`group`等)でSQLが失敗する。 | (1)存在確認をバッチ(`IN`句やまとめ取得)にするか、想定規模での所要時間を計測して許容を明記する。(2)`update`の戻り値を確認して0件なら全体をロールバックする。(3)コミット失敗時も`ImportExecutedEvent(committed=false)`を発行し、利用者に安全なエラー形式で返す。(4)識別子の引用符処理をDB方言ごとに検討するか、対象外としてスコープ判断を記録する。 | New |
| R-07 | Minor | backend/src/test/java/com/mastersmith/dataio/service/CsvImportServiceTest.java、backend/src/test/java/com/mastersmith/dataio/service/CsvExportServiceTest.java、aidlc/spaces/default/intents/260910-master-mgmt-app/construction/data-import-export/code-generation/unit-test-instructions.md | テストの網羅が薄い。`CsvImportServiceTest`は3件(全行有効、全体ロールバック、後勝ち)のみで、BR8.1のファイル形式エラー(`CsvFormatException`)・BOMなし・空ファイル・ヘッダーのみ・対象テーブル不在・行単位エラーの内容(行番号・フィールド名)を検証していない。`CsvExportServiceTest`は`filter`/`sort`(SQL組み立てと未知列の無視)、hidden列・権限外列の実出力、`CsvExportException`を検証していない(`CsvColumnDefinitionResolverTest`が単体で列除外を検証している点のみ)。`unit-test-instructions.md`は`@SpringBootTest`・config-engine内部設定DBの再利用・テスト後ロールバックと記すが、実装は`@SpringJUnitConfig`・独立H2・`DROP TABLE`で、記述と実態が一致しない。複数プロファイル横断E2E・安全失敗テスト(team.md 必須種別)は、本ユニットでは`CsvRowValidatorTest`(バリデーション)のみが該当する。 | 上記の未検証の経路をテストに追加し、`unit-test-instructions.md`を実際のテスト構成(SpringJUnitConfig・独立H2)に合わせて修正する。 | New |

### Validation Tool Results

| Tool | Result | Interpretation |
|---|---|---|
| `./gradlew :backend:test --tests "com.mastersmith.dataio.*"` | BUILD SUCCESSFUL。dataio配下のテストレポート4件(3+3+14+7=27件)がfailures=0、errors=0、skipped=0 | 既存テストは通る。ただしR-01〜R-04の欠陥はテストが通る経路の外にあり、テスト成功はそれらの不在を意味しない |
| `./gradlew :backend:spotlessCheck :backend:checkstyleMain :backend:checkstyleTest` | BUILD SUCCESSFUL | 書式・スタイル違反なし |
| source-manifest.json と現状の照合 | `backend/src/main/java/com/mastersmith/dataio/`(19ファイル)・`backend/src/test/java/com/mastersmith/dataio/`(4ファイル)が実在。`ConfigEngineApi`・`ColumnConfig`は、dataio実装コミット(f5fc12c)の後は他ユニットの整形・実装コミットで触れられているが、ビルド・テストは成功している。dataioのソースの未コミット変更はない(`git status`で確認) | U5(authentication-service)の作業でdata-import-exportのソースが破壊された形跡はない。BR8.9のイベント型・サービスの依存も解決している |
| traceability.json と実装の対応 | FR12.1・BR8.1〜BR8.10の各ターゲットファイルが実在し、記載の役割と一致(BR8.6の対象は`RowError.java`のみを指すが実質のメッセージ組み立ては`CsvRowValidator`側) | 参照は解決する。ただしBR8.1(往復可能なCSV形式)とBR8.10(ストリーミング)は、R-02・R-03のとおり実装が契約を満たしていない状態でOKと記録されている |
| 設計上の意図的受容の確認 | security-design.md: CSVインジェクション(数式)対策とファイルサイズ・Content-Type制限は、NFR2.3/NFR2.4として意図的にリスク受容 | セルの数式無害化がなくても本レビューの所見にはしない(受容済み) |

### Summary

ビルド・テスト・スタイルチェックはいずれも成功し、他ユニットの変更によるdata-import-exportの破壊は確認されなかった。一方、実装は契約の主要な性質を満たしていない。主キー不正値のファイル全体エラー化(R-01)、DATETIME列のエクスポート→インポート非往復(R-02)、PostgreSQL/MySQLでストリーミングが効かない(R-03)、再インポートによるhidden列のNULL上書き(R-04)の4件のMajorがあり、これらはH2のみ・3件のインポートテストという現状のテストでは検出されない。Majorが2件を超えるためNOT-READYとする。
