## Review

**Verdict:** READY
**Reviewer:** aidlc-architecture-reviewer-agent
**Date:** 2026-09-20T12:28:30Z
**Iteration:** 1

### Findings

| ID | Severity | Location | Finding | Required action | Status |
|---|---|---|---|---|---|
| R-01 | Major | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/nfr-requirements/reliability-requirements.md > NFR4.3 Sessionのキャッシュの整合性 | NFR4.3 は「キャッシュの古さを許容する期間はゼロ」とし、無効化のきっかけを「ログアウト・再使用検知・ユーザー無効化検知による失効、アクティブロールの選択、リフレッシュ時のアクティブロールの再確認による変更」に限定している。一方 scalability-requirements.md NFR3.2 は、キャッシュ内容に `refreshExpiresAt` を含めると定めている。リフレッシュが成功すると `refreshExpiresAt` が延長される(rules.md BR5.6、W2 手順5)が、ロールが変わらない通常のリフレッシュはNFR4.3の無効化の対象に入っていない。rules.md BR5.11 は「ロールの選択・更新・失効で無効化する」と書き、「更新」にリフレッシュが含まれるように読める。この食い違いのまま実装すると、キャッシュに古い(短い)`refreshExpiresAt` が残る。その結果、DBでは有効なSessionを認証フィルタが期限切れとして401にし続ける。リフレッシュは認証フィルタもキャッシュも経由しないため成功するのに、次のAPI呼び出しがまた401になり、リフレッシュと401を繰り返す。NFR4.3 の「ゼロ」は成立しない。 | NFR4.3 の無効化の契機に「リフレッシュ成功(`refreshExpiresAt`・`previousRefreshTokenHash` 等の更新)」を明記し、BR5.11 の「更新」の解釈を統一する。または、キャッシュに `refreshExpiresAt` を保持せず、リフレッシュ成功時は必ず該当エントリを無効化する、と要件として定める。あわせて、この経路(ロール不変のリフレッシュ後に、延長後の期限で認証フィルタが200を返すこと)をNFR8.2のテスト項目に加える。 | New |
| R-02 | Major | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/nfr-requirements/security-requirements.md > 脅威の考慮(STRIDE)のサービス拒否の行、NFR2.12、残余リスクの表 | STRIDE の DoS の行は、ログインへの大量の試行に対する対応を「同時実行の上限と2秒の打ち切り(NFR1.3)」とし、Q3=A の理由も「資源が使い果たされることは避けられる」としている。この上限が守るのはサーバーの資源(CPU・メモリ)であり、ログインの可用性ではない。認証不要の `/api/auth/login` は、実在しないメールアドレスでも `dummyVerify` で毎回フルコストのハッシュ計算を起こす(BR5.2)。この許可枠は user-management の招待受諾とも共有される(NFR1.3)。IP単位の制限もない(Q3=A)。そのため、未認証の少数の同時リクエストで許可枠を占有し続ければ、正規のログインと招待受諾が503になり続ける。NFR1.4 は「正規の集中」による503だけを述べており、敵対的な占有は残余リスク1〜9のどれにも含まれず、見直しのきっかけと担当も定義されていない。残余リスクの表が網羅的でなく、STRIDE の DoS の緩和も過大評価になっている。 | 残余リスクの表に「ハッシュ計算の許可枠の占有によるログイン・招待受諾の可用性の低下(未認証、IP制限なし)」を追加する(判断は受容、見直しのきっかけは `auth_login_hash_capacity_exceeded_total` のアラート)。STRIDE の DoS の行を「資源の枯渇は防ぐが、可用性の低下は受容」と正確に書き換える。Q3=A の理由にも同じ区別を追記する。 | New |
| R-03 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/nfr-requirements/reliability-requirements.md > NFR4.3 キャッシュの更新の順序 | 順序は「DBの更新に成功してから、キャッシュを無効化する」とだけ書かれている。DBの更新がトランザクションの中でコミット前に無効化すると、その間の読み込みが古い値を再投入する。また、読み込み(ミス)の途中で更新と無効化が入ると、古い値が残る恐れがある。「ゼロ」の保証にはコミット後の無効化と、読み込みと無効化の競合の扱いが必要だが、要件に書かれていない。 | 「コミット後に無効化する」「読み込み中の無効化で古い値を保持しない」を要件に追記する(方式はNFR Designで確定)。並行のテスト(読み込みと失効の競合)もNFR8.2に加える。 | New |
| R-04 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/nfr-requirements/reliability-requirements.md > NFR4.2、NFR4.1 ログインの成功 | ロックの「枠の返却」(補償の更新)は、ハッシュ計算の上限超過の503にだけ定められている。予約のあとで内部設定DBが障害になり、成功の更新とSessionの作成(NFR4.1で同一トランザクション化)が失敗して503を返す場合、枠は失敗として数えられたままになる。正しいパスワードの利用者でも、DBの障害が続くと5回でロックされうる。この扱いがNFR4.2に書かれていない。 | DBの障害による503でも、予約の補償を試みるか、失敗として数えたままにするか(BR5.3の安全側)を明記し、必要なら残余リスクとして記録する。 | New |
| R-05 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/nfr-requirements/performance-requirements.md > NFR1.1、NFR1.2、NFR1.4 | (a) NFR1.1 は「同時50ユーザーの条件で3秒以内(p95)を満たす」としながら、NFR1.4 は同時ログインが約 CPUコア数×6.7 件(4コアで約26件)を超えると503になると認めている。503を p95 の分母や成功率に数えるかが未定義で、同時50ユーザーのログインは条件付きでしか満たせない。(b) NFR1.1・NFR1.2 は負荷試験をせず、統合テストの計測を「記録して確認する」とするだけで、標本数・ウォームアップ・合否の基準がない。org.md は、確認済みの品質目標を通過のために緩めてはならないとしている。5msのp95の測定は、条件が定義されないと不安定になる。 | NFR1.1 に、目標の適用条件(3秒は503を除く応答の p95 で、503は NFR1.4 の限界として別扱い)を明記する。NFR1.1・NFR1.2 に、計測の条件(標本数・ウォームアップ・単独か同時か)と、目標を超えた場合の扱い(Build and Test の失敗か、記録のみか)を定める。 | New |
| R-06 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/nfr-requirements/security-requirements.md > NFR2.2、tech-stack-decisions.md > 認証の基盤の行 | (a) NFR2.2 は時計のずれの許容を設けないとするが、tech-stack-decisions.md は Spring Security・Nimbus の採用を想定する。Spring Security の JWT の標準の検証は、既定で60秒のずれを許容するため、明示的にゼロへ上書きしないと要件を満たさない。上書きがtech-stack-decisions.md にも確認事項にもない。(b) 鍵の「256ビット以上」を、環境変数の文字列で判定する際の表現(生のバイト長か、Base64か、16進か)が定義されておらず、NFR4.4 の起動時検証を Code Generation が推測することになる。 | tech-stack-decisions.md に「JWT検証の時計のずれの許容を0に設定する」を記載する。鍵の外部表現(例: Base64、デコード後32バイト以上)を要件として確定する。 | New |
| R-07 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/nfr-requirements/observability-requirements.md > NFR5.1 `auth_filter_unauthorized_total` の `reason`、アラートの暫定のしきい値 | `reason` は `expired`・`invalid`・`session_inactive` の3分類で、「Bearerトークンが無い」(BR5.11の401の原因の1つ)がどれに入るか定義されていない。`invalid` に入るなら、未ログインのアクセスや監視のプローブで、5分間10回以上のアラートが誤って発報する。また、攻撃の兆候として最も重要な「`sub` と Session の `userId` の不一致」が、形式不正などと同じ `invalid` に混ざる。 | 「ヘッダー無し」は別の値(例: `missing`)とし、アラートの対象から外す。`sub` の不一致は独立した値、または独立したカウンタとして数える。 | New |
| R-08 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/nfr-requirements/tech-stack-decisions.md > 契約追補(保留事項の一覧)、security-requirements.md > NFR2.8・NFR2.1 | 契約追補の一覧は、新規の振る舞いのうち、503・`Cache-Control`・413・`dummyVerify`・`sub` の照合・トランザクションの統合を含むが、NFR2.8 の `WWW-Authenticate: Bearer`(C4のすべての401への付与、加法的な追補)を含まない。また、NFR2.1 は認証を要しない経路を `/api/**` の3つとするが、フィルタの対象外の経路(ヘルスチェック・アクチュエータ・静的リソース)を誰が、どの認証で保護するかが未定義である。NFR5.4 は「ヘルスチェックに参加する」とだけ書いている。 | `WWW-Authenticate: Bearer` を追補一覧に加える。`/api/**` 以外の経路(ヘルスチェック・メトリクス・静的リソース)の認証の要否と、公開してよい範囲を要件に明記する。 | New |
| R-09 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/nfr-requirements/security-requirements.md > 残余リスク8・9、NFR2.9 | (a) 残余リスク8は「受容。CSPと外部スクリプトの排除で軽減」とするが、CSPの担い手は tech-stack-decisions.md の確認事項2で未確定であり、緩和策の実施者が存在しない状態で受容の根拠にしている。(b) 残余リスク9の見直し先は「鍵の入れ替えと、全Sessionの失効を行う」だが、全Sessionの失効を行う手段(管理API・SQL・起動時の設定など)が、要件にも環境の前提(NFR4.7)にも定義されていない。 | (a) 残余リスク8を、CSPの担い手が確定するまでは未緩和として記録する。(b) 全Session失効の手順の所有と、最低限の手段(例: 運用手順としてSessionを全削除する)を、NFR4.7の環境の前提に加える。 | New |
| R-10 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/nfr-requirements/security-requirements.md > NFR2.3・NFR2.5、NFR2.10、reliability-requirements.md > NFR4.5 | (a) リフレッシュのたびに有効期限を「最後の更新から30分」に延ばすため、更新を続ける限りSessionが無期限に生きる。Session全体の絶対期限(最大の寿命)を置かない判断は、[assumption] としても記録されていない。(b) NFR2.10 は413を「内容を読み込む前に」拒否するとするが、`Content-Length` の無い(chunked)リクエストでは、読み込みながらの上限の適用が必要になる。(c) NFR4.5 は「1日1回」の実行の基準(固定の時刻か、起動からの間隔か)を定めていない。 | (a) 絶対期限を置かない判断を [assumption] として記録し、見直しのきっかけを添える。(b) 上限をストリームの読み込み上限として定める。(c) 実行の基準を1文で確定する。 | New |

### Validation Tool Results

| Tool | Result | Interpretation |
|---|---|---|
| traceability.json の手動検証(NFR1〜NFR8) | PASS | requirements.md のNFR1〜NFR8の8件がすべて `upstream_ids` と `coverage` にある。NFR1.1〜1.4、NFR2.1〜2.13、NFR3.1〜3.5、NFR4.1〜4.8、NFR5.1〜5.5 は、それぞれ1つの成果物内に見出しがある。NFR7.1、NFR8.1、NFR8.2 は tech-stack-decisions.md にある。NFR6 は N/A で、正当化の文が付き、NFR6 の要件を引用していないため、自己矛盾はない |
| NFRx.y の相互参照の確認 | PASS | 成果物間で引用されるNFR番号(NFR1.2、NFR1.3、NFR2.7、NFR2.11、NFR4.1〜4.5、NFR5.1 等)は、すべて存在する。名前の一致しないメトリクスは見つからない |
| 数値の検算(ログインの時間の予算) | PASS | 50+50+2000+300+110=2510ms(約2.5秒)。再ハッシュ込みで約2.8秒(+300ms)で、3秒以内に収まる。同時ログインの概算(コア数×(2÷0.3))は、4コアで約26件で整合する(順番待ちの世代の数え方で±2件は許容範囲) |
| 保持日数の基準の確認(Q4との整合) | PASS(注記あり) | Q4の「期限または失効から7日後」に対し、成果物は `refreshExpiresAt` 基準に統一している。統合サマリーで [assumption] として確認済み(「Looks correct」)のため、逸脱とはしない |
| 既存の契約・機能設計との整合(C4・C11・C14、BR5.x) | 概ね整合 | 新規の振る舞い(503・`Cache-Control`・413・`sub` の照合・トランザクションの統合・`dummyVerify` の長さ規則)は追補一覧に載る。追補漏れは R-08 のみ。キャッシュの無効化の契機の食い違いは R-01。Q1〜Q7の確定回答との矛盾は見つからない |

### Summary

Critical はなく、Major は2件(R-01 キャッシュの無効化の契機の食い違い、R-02 ログインの可用性の低下が残余リスクに無いこと)、Minor は8件である。判定基準(Critical 0件、Major 2件以下)に従い READY とするが、R-01 は実装で確実に不具合になるため、承認前の修正を推奨する。R-02 も、Q3=A の受容の理由を正確にする目的で修正を推奨する。
