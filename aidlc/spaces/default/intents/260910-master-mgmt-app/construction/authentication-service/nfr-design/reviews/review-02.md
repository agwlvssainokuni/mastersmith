## Review

**Verdict:** READY
**Reviewer:** aidlc-architecture-reviewer-agent
**Date:** 2026-09-20T20:56:29Z
**Iteration:** 2

### Findings

| ID | Severity | Location | Finding | Required action | Status |
|---|---|---|---|---|---|
| R-01 | Major | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/nfr-design/security-design.md > NFR2.1 フィルタの適用範囲 / NFR2.2 検証の手順 | 認証不要のパスでのフィルタの振る舞いが未定義だった。 解消の確認: security-design.md NFR2.1「フィルタの適用範囲」が、順2にだけ適用し(`shouldNotFilter`)、順1・3・5では`Authorization`の有無・期限切れ・不正にかかわらず素通しと明記。Bearerスキームの大文字小文字の扱い(NFR2.2手順1)、logical-components.mdの部品表とNFR8.2のテスト(期限切れ・不正な`Authorization`で401にならないこと)も整合している | (前回)認証不要のパスでフィルタを実行しない旨を明記する。 | Resolved |
| R-02 | Major | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/nfr-design/reliability-design.md > NFR4.2 例外の変換 / security-design.md > NFR2.8 / logical-components.md > 横断的な部品の所有 | C11の呼び出しでの内部設定DB障害、およびAuthController以外のコントローラでのC14の例外(401・503)を変換する担い手がなかった。 解消の確認: `UserAccountClient`(reliability NFR4.2、logical-components 部品表)がC11の障害を変換し、`AuthCrossCuttingExceptionAdvice`(例外の型を3つに限定、コントローラは限定しない)が401・503へ変換する(security NFR2.8の表の最後の2行)。追補20番、`SessionContextService`が`SessionCache`経由で読む旨、テスト(NFR8.2)も揃っている。トランザクションの境界に由来する変換の残りの穴は、新規のR-15として扱う | (前回)`UserAccountClient`と、他のコントローラにも効く変換の担い手を定め、追補に記録する。 | Resolved |
| R-03 | Major | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/nfr-design/security-design.md > NFR2.3 条件付きの更新 / NFR2.6 | リフレッシュの更新が、並行するロール選択を古いロールで上書きしうる(失われた更新)。 解消の確認: WHERE句に`(active_role_id = :readRoleId OR (active_role_id IS NULL AND :readRoleId IS NULL))`が加わり、判定表が「ハッシュが変わった: 401・失効させない」「ハッシュは同じでロールだけ変わった: 読み直したロールにNFR2.6の表を適用して1回だけやり直す」「やり直しも0件: 401・失効させない」を定めている。ロール選択の更新が無条件(`session_id`・`status`のみ)であるため、どちらの順でも選択は失われない。並行実行のテストもNFR8.2にある。論理は、並行時にも成立すると確認した | (前回)WHERE句に読み取り時点のロールとの一致(NULLを含む)を加え、負けた場合の扱いを決める。 | Resolved |
| R-04 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/nfr-design/reliability-design.md > NFR4.1 予約型のロック 手順3 | 一意制約違反のやり直しのトランザクション境界と、予約後の想定外の例外での補償の扱いが未定義だった。 解消の確認: `AuthenticationApplicationService`がトランザクションの外で捕捉し、新しいトランザクションで最大3回やり直す。3回とも違反なら503。この違反は500にならない。予約後の想定外の例外は補償せず500(security NFR2.4手順5、reliability補償の節)で、両ファイルが一致している。トランザクションの所有(`TransactionTemplate`は`AuthenticationApplicationService`だけ、ゲート・サービスは`MANDATORY`)も、やり直しの設計と整合する | (前回)やり直しをトランザクションの外で行う形に明記し、補償の有無を決める。 | Resolved |
| R-05 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/nfr-design/security-design.md > NFR2.9 / NFR2.10 | サイズ制限フィルタの413にセキュリティヘッダーが付かない。`RequestBodyTooLargeException`の型が未定義。HSTSの前提が未記載。 解消の確認: `SecurityHeaderValues`を`SecurityFilterChain`と`ProblemDetailsWriter`が共有し、両経路の413のヘッダー一致をテストする。`RequestBodyTooLargeException`は`IOException`のサブクラスで、原因の連鎖をたどって413に変換する。HSTSは`server.forward-headers-strategy`かリバースプロキシを環境の前提とし、追補15番に記録されている | (前回)`ProblemDetailsWriter`がヘッダーを付ける。例外の型を定める。HSTSの前提を記す。 | Resolved |
| R-06 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/nfr-design/security-design.md > NFR2.1 順3〜6 / logical-components.md > 追補15番 | 認証なしで到達できる面の定義に不整合があった(順5の範囲、ヘルスのグループ、プル型メトリクスと順4、ヘルスの接続消費)。 解消の確認: 順5はGET・HEADに限定し、順6が残りを拒否。ヘルスは完全一致で5秒キャッシュ、グループなし。プル型メトリクスは順4より前に明示的に追加する形に整理され、追補15番と一致している。ただし、順5が`/h2-console`などのGETを通す点は、新規のR-16で扱う | (前回)順5を絞り、ヘルスのグループ・メトリクス・キャッシュを整理する。 | Resolved |
| R-07 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/nfr-design/reliability-design.md > NFR4.1 SQLの前提 / 補償の更新 | SQLの前提(SET句の評価順、分離レベル、時刻の加算、補償のNULL比較)が固定されていなかった。 解消の確認: H2・READ COMMITTEDを前提として固定し、MySQL・MariaDBでは書き直しが必要と明記。並行実行のテストをCIの必須の合格条件とした。`:newLockedUntil`・`:newRefreshExpiresAt`はJava側で計算して渡す。補償は`:myLockedUntil IS NOT NULL AND locked_until = :myLockedUntil`とし、ロックを設定しなかった予約の場合の結果(回数だけ戻り、ロックは自動解除まで残る)を記している。世代・回数の遷移を追ったが、二重の補償・世代の交代後の補償のいずれでも整合した | (前回)内部設定DBと分離レベルを固定し、時刻をJava側で計算し、補償のNULLの扱いを書く。 | Resolved |
| R-08 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/nfr-design/scalability-design.md > NFR3.4 auth_session | `session_id`の長さと乱数のビット数が食い違い、`sid`の生成部品が未定義だった。 解消の確認: `SessionIdGenerator`(16バイトの`SecureRandom`、Base64URL 22文字)が、security NFR2.3・scalability NFR3.4(22文字)・logical-components 部品表の3か所で一致している | (前回)生成方式を定め、列の長さと揃え、部品を部品表に加える。 | Resolved |
| R-09 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/nfr-design/observability-design.md > NFR5.1 / reliability-design.md > NFR4.4 | `auth.filter.duration`のタグ名の不一致と、キャッシュの設定値・`revoke-all-on-startup`が起動時の検証に含まれていなかった。 解消の確認: タグは`result`に統一(observability NFR5.1)。キャッシュの最大件数・TTL・`revoke-all-on-startup`が、reliability NFR4.4の検証とlogical-components NFR8.2のテストに加わった | (前回)タグ名を揃えるか[assumption]を記す。検証の一覧とテストに加える。 | Resolved |
| R-10 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/nfr-design/reliability-design.md > NFR4.2 / NFR4.3 | DB障害中の認証継続の上限(60秒)と、読み込み中のブロックの副作用が未記載だった。 解消の確認: NFR4.2に「最大60秒」とテストの期待値の整合、NFR4.3に同じビンのキーが最大3秒待たされる副作用と[assumption]の判断が記されている | (前回)上限を明記し、副作用を判断として残す。 | Resolved |
| R-11 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/nfr-design/performance-design.md > NFR1.1 計測の方法と合否 | ログインの計測条件が自己矛盾していた。 解消の確認: 同時数を「ハッシュの同時実行の上限の2倍」とし、待ち時間の見積もり(約0.6秒)と、正しいパスワードだけを使う計測用アカウント(50件など)を明記。50の同時ログインは計測しないと整理されている | (前回)ログインの同時数を式で定め、計測用のアカウントを明記する。 | Resolved |
| R-12 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/nfr-design/security-design.md > NFR2.2 鍵を漏らさない / NFR2.5 | 鍵の束縛の型と、`revoke-all-on-startup`の実行のタイミングが未定義だった。 解消の確認: 鍵は`AuthProperties`の束縛の対象にせず、`JwtKeyProvider`が`Environment`から直接読む。例外に値の断片を含めず、原因の連鎖もしない。全削除は`SmartInitializingSingleton`でFlywayの後・Webサーバーの受付の前に実行し、`ApplicationRunner`を使わない理由も記している | (前回)鍵を`toString`・束縛の失敗診断で漏らさない形にし、実行のタイミングを明記する。 | Resolved |
| R-13 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/nfr-design/performance-design.md > NFR1.3 / logical-components.md > 部品の一覧 | `open-in-view`の指定と、トランザクションの境界の所有が曖昧だった。 解消の確認: `spring.jpa.open-in-view=false`が追補14番とreliability NFR4.1・performance NFR1.3に記載され、テストもある。境界は`AuthenticationApplicationService`だけが持ち、コミット後の無効化・ログイン成功の同一トランザクションの呼び出し元も同部品と明記されている | (前回)設定を追補に加え、境界を持つ部品を1つに決める。 | Resolved |
| R-14 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/nfr-design/logical-components.md > 追補18番 | U5の`code`とU4の`errors[].message`の規約の関係が未記載だった。 解消の確認: 用途の分担(`errors`があればフィールド単位のキー、なければ`code`)と、`instance`に生のパスを入れないこと(`ProblemDetailsWriter`にも適用)が記載された。統一するかは、U12・共通基盤の確認事項として残されている(妥当) | (前回)2つの規約の関係と`instance`の扱いを追補18番に記す。 | Resolved |
| R-15 | Major | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/nfr-design/reliability-design.md > NFR4.2 例外の変換 / NFR4.1 トランザクションの境界 / observability-design.md > NFR5.1 計装の場所 | 今回の改訂で、トランザクションの境界が`AuthenticationApplicationService`の`TransactionTemplate`に移った。一方、例外の変換は「`SessionRepository`・`AccountLoginStateRepository`の呼び出しの周り」(NFR4.2)にとどまる。`CannotCreateTransactionException`(内部設定DBが落ちたときの典型的な例外)は、リポジトリの呼び出しではなく、`TransactionTemplate.execute`の開始時に投げられる。コミット時の障害(`TransactionSystemException`など)も同じである。これらは、変換の対象の一覧に載っているのに、書き込みの経路(予約・補償・ログイン成功・リフレッシュ・失効・ロール選択)では、変換の担い手がない。そのため、`AuthApiExceptionAdvice`の「分類できない例外は500」になり、NFR4.2(すべての認証系の処理で、DB障害は503)に反し、フロントエンドの503の扱い(NFR2.11)が働かない。あわせて、observability-design.mdは`auth.db.unavailable`の計装の場所を`AuthExceptionTranslator`とするが、この部品は、logical-components.mdの部品表にも、他の成果物にも存在しない(grepで確認)。`auth_db_unavailable_total`を数える担い手も、reliability NFR4.2の記載では決まっていない | 変換の担い手を、1つの部品(たとえば`AuthExceptionTranslator`)に決め、logical-components.mdの部品表に加える。その部品が、(a)`TransactionTemplate.execute`の呼び出し全体(開始・本体・コミット)、(b)`SessionRepository`の読み取り(`SessionCache`のloaderを含む)、(c)`UserAccountClient`の呼び出し、を包み、`TransactionSystemException`を対象に加えて、`AuthStorageUnavailableException`へ変換し、`auth.db.unavailable`を数える、と記す。予約の一意制約違反の捕捉(NFR4.1)は、この変換より先に行う、という順序も明記する。NFR8.2に、「トランザクションの開始の失敗」のテストを加える | New |
| R-16 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/nfr-design/security-design.md > NFR2.1 順5と「deny by default」の説明 | 順5は、`/api/**`・`/actuator/**`以外のすべてのパスへの`GET`・`HEAD`を、認証なしで許す。そのため、`/h2-console`などのGETも、規則の上では通る。一方、同節の説明は「H2コンソールなども、認証なしでは公開されない(deny by default)」と述べており、規則と一致しない。実際の防御は、本番相当のプロファイルでの無効化だけである | 説明を「規則ではなく、プロファイルでの無効化で防ぐ」と直すか、順5を、フロントエンドの静的ファイルの配置場所とSPAのフォールバックのパスに限定する(許可リスト)。どちらにするかを決めて記す | New |
| R-17 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/nfr-design/security-design.md > NFR2.1 順4・順6 / NFR2.8 | 順4・順6で拒否されたリクエストは、認証フィルタを通らず、Spring Securityの`AuthenticationEntryPoint`・`AccessDeniedHandler`が応答する。この応答のステータス(401か403か404か)・形式(ProblemDetailsか)・`code`が、NFR2.8の表にない | `ProblemDetailsWriter`を使う`AuthenticationEntryPoint`・`AccessDeniedHandler`を、`AuthSecurityConfig`に設定し、ステータス・`code`を、NFR2.8の表に加える。テスト(NFR8.2)に、拒否された経路の応答の形式を加える | New |
| R-18 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/nfr-design/security-design.md > NFR2.8 コントローラでの応答 / 他ユニットのコントローラの中で投げられる、U5の例外 | `AuthApiExceptionAdvice`(`AuthController`限定)と`AuthCrossCuttingExceptionAdvice`(全コントローラ、3つの例外の型)は、`AuthController`では、`AuthStorageUnavailableException`について、同じ例外を扱う。両者の相対的な`@Order`と、`AuthController`でどちらが優先されるかが、決まっていない。また、「`@Order`を明記する」と述べるだけで、値・基底のハンドラとの関係が記されていない | 値(または「基底より高く、`AuthApiExceptionAdvice`より低い」などの相対の関係)を定める。あるいは、`AuthApiExceptionAdvice`が、この3つの型を扱わず、`AuthCrossCuttingExceptionAdvice`に任せると決める。ハンドラの重複がないことを、テストで確認する | New |

### Validation Tool Results

| Tool | Result | Interpretation |
|---|---|---|
| stage definition の検証ツール | 該当なし(nfr-design.md は検証ツールを定めていない) | 実行対象なし |
| traceability.json の整合(手元のスクリプト) | PASS: `upstream_ids`(38件)と`coverage`の`id`が一致し、重複なし。各`target`のNFR見出しが該当ファイルに実在する | 前回から、参照は壊れていない。今回の改訂で追加された名前・追補番号も、実在する |
| 名前・追補番号の相互参照(grep) | 部分的にFAIL: `SessionIdGenerator`・`UserAccountClient`・`AuthCrossCuttingExceptionAdvice`・`SecurityHeaderValues`と追補14・15・18・20は、参照先が実在し一致。`AuthExceptionTranslator`はobservability-design.md NFR5.1にだけ現れ、部品表に存在しない | R-15として記録 |

### Summary

前回のR-01〜R-14は、いずれも、改訂後の文面で実際に閉じており(Resolved、14件)、セキュリティ・信頼性・部品表・テストの各ファイルの整合も保たれている(フィルタの適用範囲、`AuthCrossCuttingExceptionAdvice`、リフレッシュの条件付きの更新とやり直し、予約・補償のSQL、トランザクションの所有と一意制約違反のやり直しのいずれも、並行時の論理を追って矛盾を見つけなかった)。新規は、変換の担い手が`TransactionTemplate`の開始・コミットの障害を包まない点(R-15、Major)と、未定義の`AuthExceptionTranslator`への参照であり、あわせて、Minor 3件(R-16〜R-18)がある。Criticalが0件、Majorが1件(2件以下)のため、READYとする。R-15は、Code Generationの計画承認までに、成果物へ反映することを強く勧める。
