## Review

**Verdict:** NOT-READY
**Reviewer:** aidlc-architecture-reviewer-agent
**Date:** 2026-09-20T20:40:59Z
**Iteration:** 1

### Findings

| ID | Severity | Location | Finding | Required action | Status |
|---|---|---|---|---|---|
| R-01 | Major | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/nfr-design/security-design.md > 全体の構成 / NFR2.1 / NFR2.2 検証の手順 | 「全体の構成」の図では、`BearerAuthenticationFilter`は認可の規則より前に置かれ、全リクエストを通る。一方、NFR2.2の手順1は「Bearerで始まらなければ`missing`」、冒頭は「いずれかに失敗した時点で401」とある。NFR2.1の順1(login・refresh・招待受諾)・順3・順5(認証不要のパス)でフィルタがどう振る舞うかが、どこにも書かれていない。フィルタをそのまま全パスに適用すると、認証不要のパスで401になる。特にフロントエンドは、アクセストークンの期限切れ(401)や鍵の入れ替えの後に、古い`Authorization`ヘッダーを付けたまま`POST /api/auth/refresh`を呼びうる。この場合、フィルタが`expired`または`invalid`で401を返し、NFR2.2・NFR2.11が前提とする「リフレッシュで復旧する」経路が壊れる。実装者が推測で決めることになる。 | 認証不要のパス(NFR2.1の順1・順3・順5)では、フィルタを実行しない(`shouldNotFilter`)か、`Authorization`の有無・妥当性にかかわらず素通しにする、と明記する。認証を要するパス(順2)にだけ、NFR2.2の手順を適用する。ヘッダーの`Bearer`スキームの大文字小文字の扱いも記す。`AuthSecurityConfig`のテストに、「期限切れ・不正な`Authorization`を付けた`/api/auth/refresh`・`/api/auth/login`・招待受諾が、フィルタで401にならない」ケースを加える。 | New |
| R-02 | Major | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/nfr-design/reliability-design.md > NFR4.2 例外の変換 / security-design.md > NFR2.8 コントローラでの応答 / logical-components.md > 外部との境界(C14)・横断的な部品の所有 | 例外の変換の範囲が、NFR4.2の要件(認証フィルタ・ログイン・リフレッシュ・ログアウト・ロール選択のすべてで、DB障害は503)を満たさない。(a)変換の対象は「`SessionRepository`・`AccountLoginStateRepository`の呼び出しの周り」に限られる。ログインとリフレッシュが呼ぶC11の`findByEmail`・`findByUserId`・`isDisabled`は、同じ内部設定DBをU4のリポジトリ経由で読む。U4の生の`DataAccessException`は`AuthStorageUnavailableException`に変換されず、`AuthApiExceptionAdvice`の「分類できない例外は500」になる。(b)`AuthApiExceptionAdvice`は`AuthController`に限定される。しかし、C14(`getActiveRoleId`)は、list-engine(U10)・record-edit-engine(U11)のリクエスト処理の中で呼ばれる(contract-summary.md C14)。そこで投げられる`SessionNotFoundException`・`SessionExpiredException`(契約は「frontend-ui向けには401に変換」と定める、BR5.13)と、C14が内部設定DBを読んで投げうる`AuthStorageUnavailableException`を、U10・U11のコントローラの外へ変換する担い手が、この設計のどこにもない。 | (a)`AuthenticationApplicationService`(または`AuthExceptionTranslator`)が、C11の呼び出しで発生する内部設定DBの障害の例外も、`AuthStorageUnavailableException`に変換すると明記する。(b)`SessionNotFoundException`・`SessionExpiredException`→401(`auth.token.invalid`)、`AuthStorageUnavailableException`→503(`auth.service.unavailable`)を、AuthController以外のコントローラの応答にも適用する担い手(対象を限定しない`@RestControllerAdvice`か、共通基盤の基底のハンドラへの追補)と`@Order`を決め、logical-components.mdの追補一覧(共通基盤・U10・U11への要求)に記録する。あわせて、`SessionContextService`(C14)が`SessionCache`経由で読むこと、キャッシュミス時のDB障害の扱いを記す。 | New |
| R-03 | Major | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/nfr-design/security-design.md > NFR2.3 条件付きの更新 / NFR2.6 リフレッシュ時の再確認 | リフレッシュの条件付きの更新は、`active_role_id = :roleId`を書き込む。しかし、WHERE句は、`session_id`・`refresh_token_hash`・`status`・`refresh_expires_at`だけで、`active_role_id`の条件がない。`:roleId`は、更新より前に(トランザクションの外で)Sessionと、C11の`findByUserId`を読んで計算した値である(functional-spec.md W2の手順2〜5)。この読み取りと更新の間に、`PUT /api/auth/active-role`がコミットされると、選択された新しいロールが、古いロールで上書きされる(失われた更新)。キャッシュは、どちらの更新の後でも無効化されるため、DBの古い値がそのまま読み込まれる。利用者の画面(ロール選択の200)と、サーバーが権限判定に使うアクティブロールが食い違い、意図しないロールで操作が行われうる。NFR4.1は、更新の競合の扱いを目的にしているが、この競合を扱っていない。 | リフレッシュの更新のWHERE句に、読み取り時点の`active_role_id`との一致(NULLを含む比較)を加える。または、アクティブロールが変わる場合だけ`active_role_id`をSETし、その場合に限って条件を付ける。条件に負けた場合の扱い(Sessionを読み直して1回だけやり直す、または401でSessionを失効させない)を決め、判定表に記す。リフレッシュとロール選択の並行実行のテストを、NFR8.2に加える。 | New |
| R-04 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/nfr-design/reliability-design.md > NFR4.1 予約型のロック 手順3 / NFR4.2 例外の変換の対象外 | 「行がなければ作る。同時の作成で一意制約に違反したら、1回だけ手順1からやり直す」の、トランザクションの境界が決まっていない。Spring Dataのリポジトリ(`@Transactional`)を通した`DuplicateKeyException`は、参加中のトランザクションをrollback-onlyにしうるため、同じトランザクションでのやり直しは成立しない。一方、NFR4.2は`DataIntegrityViolationException`を500とする。同一のアクティブなユーザーへの初回の同時試行で、負けた側が500になると、未登録のメールアドレス(401)との違いが外部から観測できる(存在の推測)。また、予約の後、`verifyPasswordHash`(C11)がHashCapacityExceeded以外の例外を投げた場合の枠の扱いが、記されていない。 | 一意制約違反の捕捉と再試行を、`LoginAttemptGate`の中で、新しい`TransactionTemplate`の実行としてやり直す形で明記する(変換の前に捕捉し、上限の回数を決める)。予約の後に、HashCapacityExceeded以外の例外が起きた場合の補償の有無を決める。同時の初回試行のテストを加える。 | New |
| R-05 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/nfr-design/security-design.md > NFR2.9 / NFR2.10、logical-components.md > フィルタの順序 | (a)サイズ制限のフィルタは、Spring Securityのチェーンより前に登録される(logical-components.md)。`Content-Length`超過の413は、このフィルタが直接書くため、`HeaderWriterFilter`が付ける`X-Content-Type-Options`・`Referrer-Policy`・CSP・`Cache-Control: no-store`が付かない。NFR2.9の「すべてのレスポンス」「`/api/**`のすべてのレスポンス」に反する。チャンク転送の413はMVC経由でヘッダーが付くため、挙動もそろわない。(b)`RequestBodyTooLargeException`の型が未定義である。非チェック例外は、Jacksonの読み取りの中で`HttpMessageNotReadableException`に包まれるとは限らず、直接投げられる場合と、包まれる場合の両方がある。(c)NFR2.9の表は`Strict-Transport-Security`を挙げるが、アプリケーションはTLSを終端しないため、フォワードヘッダーの設定(`server.forward-headers-strategy`など)がなければ、付かない。 | (a)413を書く`ProblemDetailsWriter`が、NFR2.9のヘッダーを自前で付けるか、サイズ制限のフィルタをセキュリティのチェーンの内側の認証フィルタの直前に置く(型の依存の問題が残るなら前者)。(b)例外を`IOException`のサブクラスにするか、`AuthApiExceptionAdvice`が直接投げられた場合と包まれた場合の両方を413に変換する、と明記する。(c)HSTSは、フォワードヘッダーの設定を前提にすることか、環境側(リバースプロキシ)の担当であることを、明記する。 | New |
| R-06 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/nfr-design/security-design.md > NFR2.1 SecurityFilterChainの規則 順3〜5、logical-components.md > 追補の一覧 15番 | 認証なしで到達できる面の定義に、次の不整合と抜けがある。(a)順5は、`/api/**`と`/actuator`以外のすべてのパスを認証不要とし、メソッドも限定しない。「deny by default」は`/api/**`にしか成り立たず、将来、`/api`の外にマップされるコントローラや、H2コンソールなど、意図しない経路が認証なしで公開される。(b)順3の`/actuator/health`は完全一致で、`/actuator/health/liveness`などのヘルスグループは順4で拒否される。(c)追補15番は、プル型のメトリクスのエンドポイントを「認証を要するか、ネットワークで制限」して採用しうるとするが、順4は`/actuator/**`の残りをすべて拒否しており、矛盾する。(d)認証なしの`/actuator/health`は、内部設定DBのヘルスチェック(NFR5.4)を、リクエストごとに実行しうる。認証なしで内部設定DBの接続プールを消費でき、認証(NFR1.2のミス時・ログイン)と接続プールを取り合う。 | 順5を、GET・HEADの静的ファイルとSPAのフォールバックに限定し、それ以外は認証必須(または拒否)とする。ヘルスのグループの扱いを決める。プル型のメトリクスの採否と、順4の関係を整理する。ヘルスの応答の短い時間のキャッシュ(`management.endpoint.health.cache.time-to-live`など)か、DBのチェックを軽くする方針を記す。 | New |
| R-07 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/nfr-design/reliability-design.md > NFR4.1 予約型のロックの更新のSQL / 補償の更新 / security-design.md > NFR2.3 条件付きの更新 | 更新のSQLが、次の前提に依存するのに、固定していない。(a)SET句の各式が更新前の値で評価されること。設計はH2の性質として記すが、team.mdは内部設定DBを「例: H2」としか定めていない。MySQL・MariaDBは左から順に更新後の値で評価するため、予約のSQLでは`consecutive_failures`が二重に増える。(b)同時の更新で、行のロックの待機の後にWHERE条件を再評価する分離レベル(H2ならREAD COMMITTED)。SNAPSHOT系では、待った側が例外になる。分離レベルの記述がない。(c)`:now + :lockDuration`・`:now + :refreshTtl`は、そのまま実行できる式ではない(H2のTIMESTAMPに数値を足すと日数になる)。(d)補償の`locked_until = :myLockedUntil`は、予約がロックを設定しなかった場合、パラメータがNULLで、NULL比較になる。この場合の結果(ロックが別の予約により設定されていると、回数だけが戻り、ロックが残る)が、記されていない。 | 内部設定DBの種類と分離レベルを、設計の前提として固定し、Code Generationのテスト(同時の予約・補償・リフレッシュ)を必須の合格条件として記す。時刻の加算は、Java側で計算し、パラメータ(`:newLockedUntil`・`:newRefreshExpiresAt`)として渡す形に直す。補償の、予約がロックを設定しなかった場合の扱いを、明記する。 | New |
| R-08 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/nfr-design/scalability-design.md > NFR3.4 auth_session、security-design.md > NFR2.2 手順8 | `session_id`の列が「43文字、Base64URLの128ビット乱数」とある。128ビットのBase64URL(パディングなし)は22文字で、43文字は256ビットである。数値が食い違う。さらに、`sid`を生成する部品(`RefreshTokenGenerator`と同様の`SessionIdGenerator`など)が、logical-components.mdにも、security-design.mdにもない。NFR2.2の手順8(`sub`とSessionの`userId`の照合)と残余リスク9は、有効な`sid`を知らない攻撃者はなりすませない、という前提で成り立っており、`sid`の推測不能性が設計上の根拠である。 | `sid`の生成方式(`SecureRandom`、ビット数、文字数)を定め、列の長さと整合させる。生成の部品を、logical-components.mdの部品一覧に加える。 | New |
| R-09 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/nfr-design/observability-design.md > NFR5.1 メーターの表 / reliability-design.md > NFR4.4 | (a)`auth.filter.duration`のタグ名が`cache`(`hit`・`miss`)で、要件の`auth_filter_duration_seconds`の`result`ラベルと異なる。[assumption]の記載がない。加えて、`cache.gets`のMicrometer標準のタグ`cache`(キャッシュの名前)と、同じ名前で意味が異なる。(b)Q1=Aにより、キャッシュの最大件数とTTLは設定で変更できるとしたが、NFR4.4のfail fastの検証の一覧に含まれない。TTLが0以下、または最大件数が0以下だと、キャッシュが無効になり、NFR1.2のヒット時の目標が意味を失う。`auth.session.revoke-all-on-startup`の型の検証も同様である。 | (a)タグ名を`result`にそろえるか、差異を[assumption]として記す。(b)キャッシュの最大件数・TTL・`revoke-all-on-startup`を、NFR4.4の検証の一覧とテスト(NFR8.2)に加える。 | New |
| R-10 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/nfr-design/reliability-design.md > NFR4.2 キャッシュに有効なSessionがある場合 / NFR4.3 読み込みと無効化の競合 | (a)NFR4.2は「DBの障害の間も、キャッシュの内容で判定する」とするが、キャッシュは書き込みから60秒で失効する(scalability-design.md NFR3.2)。失効後の再読み込みは、DB障害中は503になる。この障害耐性の実際の上限(最大60秒)が、記されていない。(b)NFR4.3の、進行中の読み込みを`invalidate`が待つ性質は、CaffeineがConcurrentHashMapの計算の中で読み込むことによる(妥当な前提)。ただし、読み込みの中でDBを最大3秒待つと、コミット後の`invalidate`を呼ぶ書き込み側のスレッドと、同じビンのキーの操作が、その間ブロックされる。この副作用が、記されていない。 | (a)DB障害中の認証の継続が、最大60秒であることを、NFR4.2に明記し、テストの期待値をそろえる。(b)読み込みのブロックの副作用を、記載する。読み込み中のクエリのタイムアウトを短くするか、影響が許容範囲であることを、判断として残す。 | New |
| R-11 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/nfr-design/performance-design.md > NFR1.1 計測の方法と合否 | 計測の条件が、自己矛盾している。「各APIを、50の同時リクエストで、それぞれ200回以上」とする一方、「ログインは、上限の範囲内の同時数で計測する」ともある。ハッシュ計算の同時実行の上限は、既定でCPUコア数である。4コアでは、50の同時ログインは、待機の2秒を超えて503になる(performance-requirements.md NFR1.4の見積もり: 約26件)。ログインの計測に使うアカウント・パスワード(誤ったパスワードの繰り返しは、ロックを起こす)も未定義である。 | ログインの計測の同時数を、具体的な式(たとえば、コア数を基準にした値)で定める。計測に用いるアカウントの数と、成功するパスワードだけを使うことを明記する。 | New |
| R-12 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/nfr-design/security-design.md > NFR2.2 鍵を漏らさない / NFR2.5 全Sessionの失効の手段 | (a)`auth.jwt.secret`を、`AuthProperties`のどの型で束縛するかが、書かれていない。`String`のまま`record`で束縛すると、既定の`toString`が値を出す。また、Spring Bootの束縛の失敗の診断は、`Value:`として値を表示する。`Base64`のデコード失敗の例外も、値の断片を含みうる。(b)`revoke-all-on-startup`の実行のタイミングが決まっていない。`ApplicationRunner`は、Webサーバーが起動した後に実行されるため、その間に作られたSessionが削除されうる。 | (a)鍵の束縛を、`SecretKeyMaterial`型への専用の変換で行うか、`toString`を伏せる型にする、と明記し、束縛の失敗と`toString`の両方で値が出ないテストを加える。(b)Webサーバーがリクエストを受け付ける前(Flywayの移行の後)に、削除を実行することを明記する。 | New |
| R-13 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/nfr-design/performance-design.md > NFR1.3 資源の取得順序の不変条件 / logical-components.md > 部品の一覧 | 「ハッシュ計算の許可を待つ間、DB接続を保持しない」という不変条件が、永続化の設定に依存する。データアクセスがJPAで、`spring.jpa.open-in-view`が既定のtrueのままだと、リクエストの間、EntityManagerが束縛され、接続が保持されうる。この設定の指定が、設計にも、共通基盤への要求(追補14・15番)にもない。トランザクションの境界の所有も曖昧である。`AuthenticationApplicationService`が`TransactionTemplate`を持つとする一方、`LoginAttemptGate`(予約・補償)と`SessionService`(コミット後のキャッシュの無効化)も、それぞれ「1つのトランザクション」「コミット後」を担うとあり、ログイン成功の「同一のトランザクション」がどの部品に属するかが分からない。 | 認証のリクエストで、接続を保持しないための設定(`open-in-view=false`か、JDBCのみの使用)を、設計と追補一覧に加える。トランザクションの境界を持つ部品を、ひとつに決めて、ログイン成功のトランザクションと、コミット後のキャッシュの無効化の呼び出し元を明記する。 | New |
| R-14 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/nfr-design/security-design.md > NFR2.8 / logical-components.md > NFR7.1・追補18番 | U5は、エラーのi18nキーを、ProblemDetailsのトップレベルの拡張メンバー`code`で返す。一方、user-managementの契約追補(contract-summary.md C5の追補)は、`errors[].message`をi18nキーとし、`code`を持たない。frontend-ui(U12)が、2つの異なる規約を扱うことになる。追補18番は、`code`を加法的とだけ記し、この差異に触れていない。 | 2つの規約の関係(用途の分担、または統一)を、追補18番に明記する。ProblemDetailsの`instance`の扱い(生のパスを入れない、C5の追補)も、`ProblemDetailsWriter`に適用する旨を記す。 | New |

### Validation Tool Results

| Tool | Result | Interpretation |
|---|---|---|
| stage definition の検証ツール | 該当なし(nfr-design.md は検証ツールを定めていない) | 実行対象なし |
| traceability.json の整合(手元のスクリプト) | PASS: `upstream_ids`(38件)と`coverage`の`id`が一致し、重複なし。各`target`の`NFRx.y`は、該当の成果物に`## NFRx.y:`の見出しとして実在する | ID・見出しの参照は妥当。網羅性の問題は見つからなかった。NFR6は、上流のnfr-requirementsでN/A(画面なし)であり、対象外として妥当 |
| Q1〜Q3との整合 | PASS: Q1(60秒のTTL)・Q2(U5がSecurityFilterChainを所有)・Q3(ダミー検証の占有を受容、残余リスク10)は、全成果物で一貫して反映されている | 数値(1,000件・60秒・10秒の代替案)と残余リスクの番号(10・11)も、ファイル間で一致 |

### Summary

設計の全体は、上流の要件・Q1〜Q3・機能設計と、ほぼ整合している(予約・補償・リフレッシュのSQLの論理、コミット後の無効化、Caffeineの読み込みと無効化の競合の性質は、妥当と判断した)。ただし、(1)認証不要のパスでの認証フィルタの振る舞いが未定義でリフレッシュによる復旧を壊しうること(R-01)、(2)DB障害・Session例外の503・401への変換が、C11の呼び出しとAuthController以外のコントローラに及ばないこと(R-02)、(3)リフレッシュの更新が、並行するロール選択を上書きしうること(R-03)の3件のMajorがあり、Major 3件(2件を超える)のため、NOT-READYとする。Minor 11件は、あわせて対応すると、Code Generationで実装者が推測する箇所が減る。
