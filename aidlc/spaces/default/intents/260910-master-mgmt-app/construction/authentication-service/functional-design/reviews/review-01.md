## Review

**Verdict:** NOT-READY
**Reviewer:** aidlc-architecture-reviewer-agent
**Date:** 2026-09-20T11:18:27Z
**Iteration:** 1

### Findings

| ID | Severity | Location | Finding | Required action | Status |
|---|---|---|---|---|---|
| R-01 | Major | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/functional-design/functional-spec.md > W7、rules.md > BR5.12、entities.md > Operator | W7とBR5.12は、U2・U4・U6・U7の暫定の操作者取得を「`Operator`を読む実装」に置き換えるとする。しかし`Operator`の置き場所(どのパッケージ・どのユニットの型か)と、各ユニットへの渡し方(リクエスト属性、認証コンテキスト、共有インタフェースなど)が、どの成果物にも定義されていない。`Operator`をU5の型として置くと、U4がU5の型を読むことになり、確定済みの依存(unit-of-work-dependency.mdの「authentication-service→user-management」、C11)と合わせて U4→U5→U4 の循環になる。U2・U6・U7も、下位レイヤーから上位レイヤー(Layer 3のU5)への依存が新たに生じる。W7は「DAGと整合する」ことの確認も、記録もしていない。C4・C11・C14への追補一覧にも、`Operator`を受け渡す契約(共有型)の追加が無い。 | `Operator`を、U5に依存しない中立の場所(shared-schemaなどの共有モジュール、または、ServletのRequest属性・認証コンテキストのような、型だけを共有する仕組み)に置き、U5が値を設定し、他ユニットは読むだけであることを、`functional-spec.md`のW5・W7・追補一覧に明記する。あわせて、W7が生む依存の向きが`unit-of-work-dependency.md`のDAGと矛盾しない(循環しない)ことを、確認して記録する。契約(共有型)の追補が要るなら、追補一覧に加える。 | New |
| R-02 | Major | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/functional-design/functional-spec.md > W2 手順2・手順5、rules.md > BR5.6 | リフレッシュのローテーションが、正当な再送・同時実行と、攻撃を区別できない。(1)ネットワークの再送、または、同一Session(複数タブ)からの同時のリフレッシュで、後から到着した要求は、`previousRefreshTokenHash`と一致してSession全体が失効し、利用者は再ログインを強いられる。猶予(一定時間内の同一トークンの再送を許す、直前に発行した応答を返すなど)も、冪等性の扱いも、設計に無い。(2)同時の2件で、条件付き更新に負けた側(手順5)は「401だけ」でSessionを失効させない一方、少し遅れて到着した同じトークンの要求は、手順2で失効させる。同じ状況で、挙動が到着のタイミングによって変わり、ルールとして矛盾している。(3)クライアントは、401を「セッション終了」と扱うのが通常であり、負けた側の401の意味(再試行してよいか)も、定義されていない。この論点は、Assumptions & Open Questionsにも記録されておらず、確定事項のように書かれている。 | 同時・再送のリフレッシュの扱いを定める(例: 一定時間内の再使用の猶予、または、失敗時の応答の意味の定義)。手順2と手順5の挙動を、同じ状況で一貫させる。採用しない場合も、判断とリスク(誤失効による再ログイン)を`[assumption]`として、Assumptions & Open Questionsに記録し、人間の確認を得る。 | New |
| R-03 | Major | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/functional-design/functional-spec.md > W1 手順3・4・7、rules.md > BR5.2・BR5.15 | 「ダミーのハッシュを検証して応答時間を近づける」を、U5のどのコンポーネントが、どの手段で行うかが未定義である。C11には、ユーザーを指定せずに検証する手段が無く(`verifyPasswordHash(userId, password)`のみ)、C11の追補にも、そのためのメソッドが無い。U5が自前でハッシュを計算するなら、U4が持つハッシュ計算の同時実行数の上限(BR5.15の前提)を経由せず、メモリを多く使うハッシュの計算が、認証なしで無制限に起動できる。U4経由なら、C11の追補が要るが、追補一覧にも無い。さらに、ダミーの検証が同時実行数の上限を共有するか否かで、結果が変わる。共有しない場合、上限に達している間、503は、実在するactiveなロック外のアカウントにだけ返り、未登録・招待中・無効化済み・ロック中は401のままになる。攻撃者が負荷をかければ、503と401の違いから、存在とロックの状態を推測できる(BR5.2の目的と、BR5.15の「原因を隠さず503」が衝突する)。 | ダミーの検証の実装の場所(C11にメソッドを追加するか、U5の内部で、U4と同じ上限を共有する手段を用いるか)を決め、追補一覧に加える。ダミーの検証が上限超過になった場合の応答を定義し、503の有無が、アカウントの存在・ロックの状態を漏らさないことを、BR5.2かBR5.15に明記する。 | New |
| R-04 | Major | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/functional-design/functional-spec.md > W1 手順4〜7、rules.md > BR5.3、entities.md > AccountLoginState | ロックの判定と検証・更新が、別々の手順で、原子的でない。(1)手順4でロックの有無を確認したあと、手順5の(遅い)ハッシュ検証、手順7の更新の間に、判定は再確認されない。しきい値を超える多数の同時の試行が、ロックの設定前にすべて手順4を通過すると、しきい値のあとも検証され、正しいパスワードを引き当てれば、手順6で成功する(ロックをすり抜ける)。待機のキュー(既定で最大2秒)にいる要求は、ロックの設定後に検証される。(2)手順4の「解除済みなら`consecutiveFailures`を0として扱う」と、手順7の「原子的に1増やす」の関係が不明確である。保存値が5のままなら、解除後の最初の失敗が6になり、しきい値に直ちに達して、再ロックされる。同時の2件が、それぞれ0として扱うと、回数が失われる。(3)最初の失敗の同時実行で、AccountLoginStateの作成が競合する。この論点は、BR5.3が「回数が失われない」ことだけを求め、上のすり抜けと、解除後のリセットの原子性には、触れていない。 | BR5.3・W1に、次を追記する。(a)ハッシュ検証のあと(または検証の許可を得た直後)に、ロックの状態を再確認し、ロック中は、検証が成功しても拒否する。(b)ロックの解除後のリセットと、失敗回数の増加を、1つの原子的な更新(条件付きの更新や、初回の作成を含む、upsert相当)として定義する。 | New |
| R-05 | Major | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/functional-design/functional-spec.md > W2 手順4・6、Assumptions & Open Questions(1件目の[open question])、rules.md > BR5.10、traceability.json > FR4.2 | BR5.10は、リフレッシュ時にサーバー側のアクティブロールを、クライアントに知らせずに、null(または自動選択)へ変更する。一方、C4のリフレッシュのレスポンス(追補2)は、トークンだけで、ロールの一覧も、現在のアクティブロールも返さない。ページの再読み込みのあと、フロントエンドは、選択可能なロールも、現在のアクティブロールも知る手段が無く、FR4.2の「選択したロールを、ヘッダーに常時表示する」を満たせない。ロールが外されてnullに戻った場合は、フロントエンドが古いロールを表示し続け、操作が403になる。この論点は、[open question]として、U12の機能設計に先送りされているが、C4の所有者はU5であり、C4の追補の内容(レスポンスへの追加か、`GET /api/auth/session`のような新API)は、U5が決めるべき事項である。それにもかかわらず、traceability.jsonは、FR4.2を`OK`としている。 | リフレッシュのレスポンスへのロール情報の追加、または、セッション情報を返すAPIの追加を、このステージで確定し、追補一覧に含める。決められない場合は、FR4.2の対応を`OK`から、保留を示す状態に改め、決定の期限と担当を明記する。 | New |
| R-06 | Major | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/functional-design/functional-spec.md > W5 手順5・追補一覧、rules.md > BR5.12・BR5.13、contract-summary.md > C10(`canAccessScreen(activeRoleId: string, screenKey)`) | BR5.12は、アクティブロールがnullのユーザーが、permission-engineの判定の結果として拒否される、または、RBAC設定が空の間の例外で、設定インポート画面へ到達できるとする。しかし、C10は、`activeRoleId: string`を必須として受け取り、nullの扱い(例外か、権限なしか、RBAC空の例外を通すか)を定めていない。追補一覧に、C10・permission-engine(U3)の変更が無い。「呼び出し元がnullを権限なしとして扱う」(BR5.13)は、各呼び出し元の慣習に依存し、U3への呼び出しの前に止めるなら、RBAC設定が空の間の例外(Q7の目的)に、初期管理者が到達できない。仕様が二択のまま残っており、権限判定の統一が保証されない(project.md Mandatedの、サーバー側での再検証の信頼性に関わる)。 | nullを、(a)呼び出し元が、U3の呼び出しの前に処理する、または、(b)U3が受け入れて、判定する、のどちらとするかを決める。(b)ならC10への追補と、U3の変更を、追補一覧に加える。(a)なら、RBAC設定が空の間の例外を、初期管理者が受ける経路を定義する。 | New |
| R-07 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/functional-design/traceability.json > coverage、unit-of-work-story-map.md(FR2.3はU4に割り当て) | BR5.7・BR5.16は、FR2.3の「以後の再認証はできない」を担保する、U5側の実装であり、W1・W2・rules.mdのsourceにも、FR2.3が挙がっている。しかし、traceability.jsonのcoverageに、FR2.3が無い(story-mapがFR2.3をU4だけに割り当てているため)。FR3.3・FR3.4も、BR5.10〜BR5.12のsourceに挙がる。U5が担う部分がありながら、どこにも対応が記録されていない。 | traceability.jsonに、FR2.3(U5の担当分: 更新時の`isDisabled`の確認)を、部分対応として追加する。story-mapとの関係(担当の分担)を、target欄に明記する。 | New |
| R-08 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/functional-design/functional-spec.md > 追補一覧の1番、contract-summary.md > C4(既知の未解消フォローアップ)、unit-of-work.md > U5 | contract-summary.mdは、FR2.7の文言の修正を「Functional Design着手前」に完了させるとしており、unit-of-work.mdは「機能設計で必ず解消する」としている。しかし、本設計は、修正を「Code Generationの計画承認までに反映する」と、後ろに送っている。同じ性質の追補(C4・C11・C14・モックアップ)は、コード生成の時点での反映で妥当だが、FR2.7だけは、上流の契約が、機能設計での解消を、明示的に求めている。 | FR2.7の文言の修正を、このステージの承認ゲートまでに、要件定義書へ追補として反映する。後ろに送る場合は、上流の記述との差異を記録し、人間の確認を得る。 | New |
| R-09 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/functional-design/functional-spec.md > W3、rules.md > BR5.8 | ログアウトは、有効なアクセストークンを要する(BR5.11)。アクセストークンの有効期限(10分)が切れたあとは、ログアウトが401になり、リフレッシュトークン(最大30分)を失効できない。共用端末で、利用者がログアウトしても、残りの期間、そのリフレッシュトークンが有効なままになる。frontend-uiへの要求(追補7)にも、その扱いが無い。 | 期限切れのアクセストークンでのログアウトの扱い(例: ログアウトの前に、フロントエンドがリフレッシュする要求を、追補7に加える。または、リフレッシュトークンによるログアウトを許す)を決める。 | New |
| R-10 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/functional-design/rules.md > BR5.3・BR5.4、functional-spec.md > Assumptions & Open Questions | 第三者が、同一アカウントへ5回の誤ったパスワードを、15分ごとに送り続けることで、初期管理者を含む任意のアカウントを、継続的にロックアウトできる(Q10=Aで、管理者による解除の手段も無い)。1つのパスワードを、多数のアカウントに試す方式(パスワードスプレー)にも、アカウント単位のロックは有効でない。`/api/auth/login`に対するレート制限の要否は、NFR Designに先送りされているが、ロックアウトの悪用は、残余リスクとしても記録されていない。 | 残余リスクとして、Assumptions & Open Questionsに記録し、NFR Designへの引き継ぎ事項(レート制限、初期管理者の保護)を明記する。 | New |
| R-11 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/functional-design/rules.md > BR5.4・BR5.11・BR5.13、functional-spec.md > W2 手順4 | Sessionの有効性の定義が、認証フィルタ(BR5.11: 署名・有効期限・`status=active`)とC14(BR5.13: revoked・リフレッシュの有効期限の経過)で異なる。アクセストークンの有効期限が、リフレッシュの有効期限を超えない保証(BR5.4の設定検証)が無く、設定次第で、2つの経路の結果が食い違う。また、W2手順4は「含まれなくなっていたら」null化・自動選択するとあるが、アクティブロールが最初からnullで、リフレッシュの時点で、ロールがちょうど1つになっていた場合(0個から1個)の扱いが、文言から読み取れない。 | BR5.4に、アクセストークンの有効期限がリフレッシュの有効期限以下であることの検証を加える。BR5.11とBR5.13の、有効性の定義を一致させる。W2手順4のnullの場合を明記する。 | New |

### Validation Tool Results

| Tool | Result | Interpretation |
|---|---|---|
| (ステージ定義に検証ツールの記載が無く、また、設計ステージのため、実行しない) | N/A | 参照の確認は、contract-summary.md(C4・C10・C11・C14)、unit-of-work-dependency.md、unit-of-work-story-map.md、requirements.mdの手動照合による |

### Summary

ルール・状態遷移・要件対応は、おおむね整っているが、`Operator`の置き場所が定義されず、U4→U5→U4の循環を招く恐れがある点(R-01)、リフレッシュの再送・同時実行(R-02)、ダミーのハッシュ検証と503が、存在・ロックの推測の経路になる点(R-03)、ロックの判定の原子性(R-04)、FR4.2を満たすためのC4の追補の先送り(R-05)、C10のnullの扱いの欠落(R-06)の、5件以上のMajorがあるため、NOT-READYとする。
