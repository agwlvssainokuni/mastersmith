## Review

**Verdict:** READY
**Reviewer:** aidlc-architecture-reviewer-agent
**Date:** 2026-09-20T11:50:22Z
**Iteration:** 2

### Findings

| ID | Severity | Location | Finding | Required action | Status |
|---|---|---|---|---|---|
| R-01 | Major | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/functional-design/functional-spec.md > W5 手順4・W7「ユニットの依存関係との整合」・追補一覧9番、rules.md > BR5.12、entities.md > Operator | (初回)`Operator`の置き場所と受け渡しが未定義で、U4→U5→U4の循環の恐れがあった。(再確認)`Operator`・`OperatorContext`を中立の共有契約C15として置き、U5が値を設定・他ユニットは読むだけとする記載が、W5・W7・BR5.12・entities.md・追補一覧9番で一致している。W7に、依存が「U2・U4・U6・U7からC15への読み取りだけ」で、authentication-service→user-management(C11)と循環せず、DAG(Layer 0〜6)が変わらないことの確認が記録された。`unit-of-work-dependency.md`のDAG(U5はLayer 3、U4はLayer 2)とも矛盾しない。C15の所有者・実装Boltの残課題は、新規のR-14として分離した | なし(C15の所有と実装Boltの明確化はR-14で扱う) | Resolved |
| R-02 | Major | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/functional-design/functional-spec.md > W2 手順2・手順5、rules.md > BR5.6、Assumptions & Open Questions(リフレッシュの再送の猶予) | (初回)再送・同時実行と攻撃の区別が無く、手順2と手順5で挙動が矛盾していた。(再確認)猶予(既定10秒、`application.yml`)を導入し、猶予内の`previousRefreshTokenHash`の提出と、条件付き更新に負けた側の両方が「401・Session維持」で一致した。猶予を超えたときだけ失効させる。トレードオフ(最初の応答を失ったクライアントは再ログイン)は、`[assumption]`と残余リスク表の4番に記録された。frontend-uiへの要求(追補7の(b))も整合している。猶予による盗用検知の遅れと、Q5=Aの確定回答(再使用は即失効)からの逸脱は、記録が不足しているためR-15で扱う | なし(R-15参照) | Resolved |
| R-03 | Major | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/functional-design/functional-spec.md > W1 手順8・追補一覧4番、rules.md > BR5.2・BR5.15 | (初回)ダミー検証の実装場所が未定義で、503が存在・ロック状態を漏らす経路になっていた。(再確認)C11に`dummyVerify(rawPassword)`を追補し、実際の検証と同じコスト・同じ同時実行数の上限を共有させ、上限超過は実際・ダミーのどちらでも`HashCapacityExceededException`から503に統一した(BR5.2・BR5.15・W1手順5と8・追補一覧4番が一致)。代替案(U5が自前でハッシュ計算)を採らない理由も、`[assumption]`に記録されている | なし | Resolved |
| R-04 | Major | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/functional-design/functional-spec.md > W1 手順4〜7、rules.md > BR5.3、entities.md > AccountLoginState | (初回)ロックの判定と検証・更新が原子的でなく、しきい値のすり抜けと、解除後リセットの競合があった。(再確認)予約型(検証の前に、1つの原子的な更新で枠を確保。ロック解除後のリセットと、初回のupsertを同じ更新に含む)に改められ、同時の試行がしきい値を超えて検証されないこと、解除後の最初の失敗が直ちに再ロックされないことが、規則(1)〜(4)から読み取れる。元の指摘は解消した。ただし、予約型の採用により新しい欠陥(ロックの永続化)が生じたため、R-12として分離した | なし(R-12・R-13参照) | Resolved |
| R-05 | Major | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/functional-design/functional-spec.md > W2 手順4・6、追補一覧2・3番、rules.md > BR5.6・BR5.10、traceability.json > FR4.2 | (初回)C4のリフレッシュのレスポンスにロール情報が無く、FR4.2を再読み込み後に満たせなかった。(再確認)C4のリフレッシュのレスポンスに`roles`・`activeRoleId`、ログインに`activeRoleId`が追補され(追補一覧2・3番、BR5.6・BR5.10・W2手順6)、traceabilityのFR4.2の記述もこれを指している。契約の現行の記述(ログインは`roles`あり、リフレッシュは`accessToken`のみ)との差分が、追補として明示されている | なし | Resolved |
| R-06 | Major | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/functional-design/functional-spec.md > W5 手順5・W7・追補一覧6番、rules.md > BR5.12・BR5.13 | (初回)C10のnullの扱いが未定義で、二択のまま残っていた。(再確認)(b)案に確定し、`canAccessScreen`・`resolveEffectivePermission`がnull・空をNONE(fail closed)として扱い、RBAC空の間の`config-import-export`の例外を`activeRoleId`にかかわらず適用すること、呼び出し元はnullを自前で401にしないこと、C14のnull返却が、W5・W6・W7・BR5.12・BR5.13・追補一覧5・6番で一致している。U3の変更は追補一覧6・8番に含まれる | なし | Resolved |
| R-07 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/functional-design/traceability.json > coverage FR3.1・FR4.2 | (初回)FR2.3のU5側の担当が、traceabilityに無かった。(再確認)独立の行ではなく、FR3.1のtarget欄に「FR2.3(story-mapではuser-managementに割り当て)の、U5側の分担として、BR5.7・BR5.16を担う」と明記された。担当の分担は追跡できるため、解消とみなす | なし | Resolved |
| R-08 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/inception/requirements-analysis/requirements.md > 末尾の追補節、functional-spec.md > 追補一覧1番 | (初回)FR2.7の文言の修正が後ろに送られていた。(再確認)`requirements.md`の末尾に「追補(Construction: authentication-service Functional Designでの確定)」があり、FR2.7を`application.yml`方式とする内容で、元の記述は残されている。追補一覧の1番・BR5.4・Assumptions & Open Questionsとも一致している。確認は本ステージの承認ゲートで得る旨も記録されている | なし(承認ゲートで人間が内容を確認する) | Resolved |
| R-09 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/functional-design/functional-spec.md > W3・追補一覧7(c)・残余リスク3番、rules.md > BR5.8 | (初回)期限切れのアクセストークンでは、ログアウトできなかった。(再確認)W3・BR5.8にフロントエンドが先にリフレッシュする要求を置き、追補7の(c)と残余リスク表の3番に、残余(リフレッシュトークンが最大30分有効)を受容として記録した。判断・引き継ぎが明確である | なし | Resolved |
| R-10 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/functional-design/functional-spec.md > 残余リスク1・2番 | (初回)ロックの悪用・パスワードスプレーが、残余リスクとして記録されていなかった。(再確認)残余リスク表の1・2番に、受容(MVP)とNFR Designへの引き継ぎ(レート制限、初期管理者の保護、検知メトリクス)が記録された | なし | Resolved |
| R-11 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/functional-design/rules.md > BR5.4・BR5.10・BR5.11・BR5.13 | (初回)Sessionの有効の定義の不一致と、ロール自動選択のnullの場合の欠落があった。(再確認)Sessionの有効(statusがactiveかつ`refreshExpiresAt`が未経過)が、BR5.11・BR5.13・W5手順3・W6手順2で同一に定義された。BR5.4にアクセストークンの有効期限≦リフレッシュトークンの有効期限の起動時検証が加わり、この検証の理由(両経路の食い違いの防止)も整合する。BR5.10は(1)〜(4)で、未選択から0→1個・複数→1個の場合を網羅した | なし | Resolved |
| R-12 | Major | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/functional-design/rules.md > BR5.3 規則(4)・検証の途中の中断の扱い、functional-spec.md > W1 手順4〜7・状態遷移(AccountLoginState) | 予約型のロックに、ロックが永続化する状態がある。ロック(`lockedUntil`)は「失敗の確定」の更新でしか設定されない一方、`consecutiveFailures`は予約の時点で増える。しきい値目前(例: 4回失敗)の状態で、5番目の試行が枠を確保した(回数が5になり、`lockedUntil`は空のまま)あと、失敗の確定の前に中断(プロセスの再起動・DB障害・想定外の例外・スレッドの中断)が起きると、状態は「回数=しきい値、`lockedUntil`=空」で固定される。BR5.3は、この場合の枠を「失敗として数えたまま(安全側)」と定めるため、回数は戻らない。以後のすべての試行は、規則(4)(回数がしきい値以上で`lockedUntil`が空なら確保しない)により枠を確保できず、ロックの設定も、成功によるリセットも、自動解除(`lockedUntil`の経過)も、起きない。そのアカウント(初期管理者を含む)は永続的にログインできず、Q10=Aにより、管理者による解除の手段も無い(内部設定DBの手作業の修正だけが復旧の手段になる)。W1の状態遷移の図・テキストにも、この「予約済みで未確定」の状態と、その終了の条件が無い。予約型の採用(R-04の解消)に伴って導入された欠陥である | `AccountLoginState`が、いかなる中断でも、時間の経過で必ず通常に戻るようにする。案: 予約の更新で回数がしきい値に達した時点で、同じ更新の中で`lockedUntil`(現在時刻+ロック時間)も設定する(ロックの設定を予約と原子的にする。検証に成功すればリセットされる)。または、予約の日時(例: `lastAttemptAt`)を持たせ、規則(4)の「検証の途中」が、検証の最大時間(ハッシュ計算の待機の上限+計算時間)を超えて続く場合は、確保できる(ロックとみなす、またはリセットする)と定める。あわせて、状態遷移の図・テキストに、この状態を加える | New |
| R-13 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/functional-design/rules.md > BR5.3(枠を返す=1戻す)・BR5.15 | 補償の更新(枠を返す「1戻す」)の条件が定義されていない。(a)検証の途中に、別の試行の成功によるリセット(回数0)や、ロックの自動解除後の新しい世代の予約が起きたあとに、無条件に1戻すと、回数が負になる(entities.mdの`min: 0`に違反)、または新しい世代の失敗の回数を1つ失う。(b)`HashCapacityExceededException`は、攻撃者が負荷で誘発できるため、補償の誤りは、実際の推測の回数を数え損ねる方向に働きうる。補償の更新が失敗した場合のふるまい(BR5.3は「失敗として数えたまま」)は、R-12の状態につながる | 補償の更新を、条件付き(回数が0より大きく、かつ予約した世代(ロックの世代・リセットの回数など)が変わっていない場合だけ1戻す)と定義する。世代を持たせない場合は、その簡略化(回数の数え損ねを許容する範囲)を、`[assumption]`として記録する | New |
| R-14 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/functional-design/functional-spec.md > 追補一覧4・8・9番、Assumptions & Open Questions(C15)、inception/contract-design/contract-summary.md > Contracts Table・Contract Ownership Rules | 追補の実装の担い手が曖昧である。(1)C15の所有者が「共通基盤」とされているが、Contract Ownership Rules(契約はプロバイダーのユニットが所有)と、契約表(所有者=ユニット)に、「共通基盤」というユニットは無い。パッケージ・所有は、Code Generationの計画で確定するとされている。(2)追補一覧4番のC11の追補(`findByUserId`・`dummyVerify`)は、「user-managementが実装する」とされているが、8番(U5のCode Generationの範囲に含める他ユニットの変更)は、U4を、`CurrentOperatorProvider`の置き換えだけに挙げている。U4・U3(実装済みのユニット)の追補を、どのBolt・どのCode Generationで実装するかが、読み取れない。(3)`contract-summary.md`の「既知の未解消フォローアップ」(C4のFR2.7)は、要件の追補後も、未解消の記述のままである | 追補一覧に、追補ごとの実装の担い手(どのユニットのCode Generationで実装するか)と、C15の所有者(暫定でも、どのユニットが提供者になるか)を明記する。`contract-summary.md`のC4のフォローアップの状態は、承認後の反映の対象に加える | New |
| R-15 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/authentication-service/functional-design/functional-spec.md > 残余リスク4番・Assumptions & Open Questions(リフレッシュの再送の猶予)、functional-design-questions.md > Q5、rules.md > BR5.6・entities.md > Session.previousRefreshTokenHash | 猶予の残余リスクの記録が、可用性(再ログイン)の側だけで、セキュリティの側が無い。(1)猶予内に、盗まれた古いトークンが提出されても、Sessionは失効せず、盗用の検知が、猶予を超えた再使用まで遅れる(この提出は401で、新しいトークンは得られないため、被害は限られるが、遅れは記録すべきである)。(2)`previousRefreshTokenHash`は直前の1世代だけを保持するため、2世代以上前の盗まれたトークンは、「未知」と区別されず、盗用の検知にならない(この制限は、Q5=Aの当初の「無効になったトークンの再使用は失効」よりも狭い)。(3)猶予は、人間が確認したQ5=Aの回答(すでに無効になったトークンの再使用は、直ちにSessionを失効)からの逸脱であり、`[assumption]`として記録されているが、Q5=Aとの差異という形では明記されておらず、承認ゲートで、その逸脱を、人間が確認する必要がある | 残余リスク表に、猶予内の盗用の検知の遅れと、1世代だけの保持による検知の範囲の限界を追加し、Q5=Aとの差異(猶予の分だけ、即時の失効から緩めている)を、`[assumption]`の記述に明記する。承認ゲートで、人間の確認を得る | New |

### Validation Tool Results

| Tool | Result | Interpretation |
|---|---|---|
| (ステージ定義に検証ツールの記載が無く、設計ステージのため、実行しない) | N/A | 参照の確認は、contract-summary.md(C4・C10・C11・C14・Contract Ownership Rules)、unit-of-work-dependency.md(DAG、Layer 0〜6)、requirements.md(FR2.7の追補)、functional-design-questions.md(Q2・Q5・Q10)、および、前回の所見(review-01.md)との手動の照合による。C15はcontract-summary.mdの契約表に未反映(追補一覧の9番として、承認後の反映とされている)ことを確認した |

### Summary

前回の11件(Major 6・Minor 5)は、成果物の記述の間(W1〜W7・BR5.x・entities.md・追補一覧・traceability)の整合まで含めて解消を確認した。C15・C10・C11・C4の追補は、DAGを変えず、循環も生じない。一方、予約型のロック(BR5.3)の採用により、予約後・失敗の確定前の中断で、「回数=しきい値、`lockedUntil`=空」の状態が永続化し、管理者の解除手段も無い(Q10=A)ため、アカウントが永久にロックされる欠陥(R-12、Major)が生じている。Criticalは0件、Majorは1件(≦2件)のため、判定はREADYとするが、R-12は承認の前に、`lockedUntil`を予約の更新と原子的に設定するなどの修正を、強く推奨する。R-13〜R-15(Minor)は、実装の担い手の明確化と、残余リスクの記録の追加である。
