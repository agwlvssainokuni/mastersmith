## Review

**Verdict:** READY
**Reviewer:** aidlc-architecture-reviewer-agent
**Date:** 2026-09-21T03:20:10Z
**Iteration:** 1

### Findings

| ID | Severity | Location | Finding | Required action | Status |
|---|---|---|---|---|---|
| R-01 | Major | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/config-engine/functional-design/rules.md > BR1.12(b); aidlc/spaces/default/intents/260910-master-mgmt-app/construction/config-engine/code-generation/traceability.json > BR1.12; aidlc/spaces/default/intents/260910-master-mgmt-app/construction/config-engine/code-generation/code-summary.md > 計画からの逸脱 | BR1.12は(a)型名の論理型への正規化と(b)物理層のSQL生成方言(クォーティング・LIMIT/OFFSET構文等)をlist-engine/record-edit-engineへ提供することの2点を要求している。実装は(a)のみ(RdbmsTypeNormalizer)。(b)に相当するものは`RdbmsDialect`の3値enum(POSTGRESQL/MYSQL/MARIADB)だけで、`ConfigEngineApi`(C9)にも`backend/src/main/java/com/mastersmith/config`配下にも、方言情報を返すメソッド・型が存在しない(`grep -rn "quote\|dialect" config/store config/dto`はTableConfigDraft.dialectとnormalize呼び出しのみ)。それにもかかわらずtraceability.jsonはBR1.12を「OK / RdbmsTypeNormalizer.java」とし、code-summary.mdは「計画からの逸脱: なし」と記載している。code-generation-plan.mdはBR1.12を「型名正規化」のみへ縮小して割り付けており、(b)の除外は明示されていない。(b)が未提供なので、後続のlist-engine/record-edit-engineがSQL方言を取得する経路がなく、C9契約にも存在しない。 | (b)を実装し契約(C9)へ追補するか、(b)を本ユニットの範囲外とする決定を、rules.md・functional-spec.md・traceability.json(status・target)・code-summary.mdの逸脱欄に、根拠と引き受けユニット(list-engine/record-edit-engineのいずれか)とともに明示する。現状のtraceabilityの「OK」は実態と合わないため修正する。 | New |
| R-02 | Major | backend/src/main/java/com/mastersmith/config/entity/ColumnConfig.java > コンストラクタ(行114-133); backend/src/main/java/com/mastersmith/config/store/ConfigModelStore.java > importConfigSet(行205-258); aidlc/spaces/default/intents/260910-master-mgmt-app/construction/config-engine/functional-design/rules.md > BR1.14 violation_behaviour | ColumnConfigには`columnConfigId`を指定できるコンストラクタ・setterがなく(全コンストラクタが`UUID.randomUUID()`を採番。TableConfigは主キー指定可)、`importConfigSet`(W5)がColumnConfigを既存のIDで上書き更新する経路が型の上で成立しない。呼び出し元(config-import-export、U9)が取れる手段は、(1) 新しいランダムIDで構築する: DBの一意制約`uk_column_config_table_column(table_config_id, column_name)`(V1)に違反し、ConfigValidationException(フィールド単位・all-or-nothing、BR1.1/BR1.11)ではなく生のDataIntegrityViolationExceptionになる、(2) `getExportableConfigSet()`が返すキャッシュ内の共有インスタンスをsetterで書き換えて渡す: ConfigCacheの「不変スナップショット」(performance-design.md)が実際は可変エンティティを保持しているため、検証失敗時にも稼働中キャッシュが汚染される、のいずれかしかない。また、BR1.14の挙動(importConfigSet経由のisPrimaryKey変更は無視して既存値を維持する)は実装されていない。`importConfigSet`は`saveAll`で呼び出し元のインスタンスをそのままマージし、publicな4引数コンストラクタで`isPrimaryKey=true`を構築して渡せば既存値を上書きする。traceability.jsonの「変更不可能であることを確認済み」は、setterが無いというconfig-engine内の観点に限った記述に過ぎない。テストも、ColumnConfigのimportConfigSetは全てMockito上の新規IDでの検証(ConfigModelStoreTest 行345-433)で、実DB・既存ColumnConfigの上書き・isPrimaryKey保持の検証は存在しない。isPrimaryKeyはU8のCSV upsert判定(INSERT/UPDATE)の根拠なので、影響が下流に及ぶ。 | (a) ID指定でColumnConfigを再構築できる経路(パッケージ内のファクトリ、またはimportConfigSet側でtableConfigId+columnNameによる既存行への突き合わせとマージ)を用意する。(b) importConfigSetで、既存行がある場合はisPrimaryKeyを既存値で固定する処理を実装する。(c) 実DBで、既存のColumnConfigを上書きするimportConfigSetと、isPrimaryKey変更の無視、検証失敗時にキャッシュ・DBが変化しないことを検証するテストを追加する。実装しない場合は、W5のColumnConfig上書きをU9側の責務としてfunctional-spec.md・contract-summary.md(C9)へ明記する。 | New |
| R-03 | Minor | backend/src/main/java/com/mastersmith/config/store/ConfigModelStore.java(行163・172・225・237・249); backend/src/main/java/com/mastersmith/config/translation/TranslationStore.java > upsert(行73); aidlc/spaces/default/intents/260910-master-mgmt-app/construction/config-engine/functional-design/functional-spec.md > Assumptions & Open Questions(行133) | W5(importConfigSet)・W6(TranslationStore.upsert)の`ConfigChangedEvent.actor`は依然として`"system"`固定である。U5が共通基盤の契約C15(`OperatorContext`)を用意したが、C15のconsumersにconfig-engineは含まれず、U2・U3・U4・U6・U7が更新された一方でU1は未対応。project.md Mandated(監査ログは操作者を記録する)を利用者操作について満たしていない。functional-spec.mdに`[open question]`として開示済みで、現時点でW5・W6を呼ぶ利用者操作の呼び出し元(U9)は未実装のため、実行時の違反はまだ顕在化していない。 | U9のCode Generation着手前に、config-engineをC15のconsumersへ追加する合意(C15の変更規則による)をするか、C9のimportConfigSet・upsertにactorを渡す契約追補を行い、actorを伝搬する。functional-spec.mdのopen questionをその時点で解決済みに更新する。 | New |
| R-04 | Minor | backend/src/main/java/com/mastersmith/audit/event/AuditLogEventMapper.java > fromConfigChangedEvent(行42-52)(他ユニットU7の統合点); backend/src/main/java/com/mastersmith/config/event/ConfigChangedEvent.java | ConfigChangedEventはtargetType/targetId/beforeValue/afterValueを運ぶが、唯一の購読側マッパーは依然として`"ConfigEngine"`固定・`event.target()`・beforeValue/afterValue=nullのみを使う(git log上、マッパーはaudit-logging・user-managementの各コミット以降変更なし)。このため、W2のドラフト取り込みでも、project.md Mandated(変更前後の値の記録)は監査ログにエンドツーエンドでは記録されない。config-engine側の発行内容は正しく、ギャップはU7側の未追従である。 | U7側のマッパーを新フィールドを読むよう改修する追跡Boltを起票し、C9(または新設のイベント契約)へConfigChangedEventのペイロードを明記する。人間の承認者に、それまで「変更前後の値」が実質未記録であることを明示する。 | New |
| R-05 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/config-engine/functional-design/rules.md > BR1.13(operation); backend/src/main/java/com/mastersmith/config/event/ConfigChangeOperation.java | BR1.13はoperationを新規作成ならCREATED・更新ならUPDATEDとすると定めるが、実装は呼び出し種別(DRAFT_IMPORTED/CONFIG_SET_IMPORTED/TRANSLATION_UPSERTED)のままである。code-summary.md・traceability.jsonに既知の逸脱として開示済みで、beforeValueのnull有無から新規/更新は判別できる。ただし、ルール本文そのものは充足していない。 | rules.md BR1.13を実装に合わせて改訂するか、CREATED/UPDATEDを実装する。 | New |
| R-06 | Minor | backend/src/main/java/com/mastersmith/config/validation/ConfigValidator.java > validateAll; backend/src/main/java/com/mastersmith/config/store/ConfigEngineStartupRunner.java | `ColumnConfig.tableConfigId`は文字列でFK制約もなく(V1: `table_config_id VARCHAR(255)`、外部キー無し)、validateAllは必須項目・BR1.4のみを検証する。存在しないtableConfigIdを持つColumnConfigをimportConfigSetで取り込んでも、起動時の全件検証でも検知されない(孤児カラムの静かな混入)。 | importConfigSet・起動時検証でColumnConfig.tableConfigIdの参照先の存在を検証する(BR1.1のfail-fast)。または孤児を許容する旨を明記する。 | New |
| R-07 | Minor | backend/src/main/java/com/mastersmith/config/event/ConfigChangeSnapshots.java; backend/src/main/java/com/mastersmith/config/cache/ConfigCache.java | (1) スナップショットのJavadocは「イミュータブルなMap」と称するが、実体は`LinkedHashMap`(可変)で、validationRule・choiceOptions・fkReferenceは可変オブジェクトの参照を共有する浅いコピー。(2) ConfigCacheのスナップショットも、Map/Listは不変だが、保持するエンティティ(公開setterあり)は可変で、getColumnConfigs等の戻り値を通じて利用側から書き換えられる(R-02(2)の前提)。 | `Collections.unmodifiableMap`・深いコピー、またはコピーを返す設計へ改める。または、記述を実態(浅いコピー・可変エンティティ)に合わせる。 | New |
| R-08 | Minor | backend/src/test/java/com/mastersmith/config/store/ConfigModelStorePerformanceTest.java; backend/src/test/java/com/mastersmith/config/store/ConfigEngineFailFastStartupTest.java; backend/src/main/resources/db/migration/V1__baseline_existing_schema.sql(translation_entry.text) | (1) 性能テストは1回計測の50ms未満のみで、p95相当でも、NFR1.2(reload後にDBアクセスしないこと)の検証でもない。(2) 起動時fail-fastテストは`MastersmithApplication`全体(他ユニットの設定を含む)を2回起動しており、他ユニットの起動設定の変更で失敗し得る結合を持つ(今回のU5追加後も成功)。ConfigValidationExceptionの有無しか見ず、フィールド単位エラー内容は検証しない。(3) `translation_entry.text`は`VARCHAR(255)`だが、TranslationEntry側に長さ制約・検証がなく、255文字超のテキストはDB制約違反になる。 | 性能テストに繰り返し計測またはDBアクセス無しの検証を追加する。fail-fastテストで`FieldError`の内容も検証する。TranslationEntry.textの長さをV1・検証の両方で明確にする。 | New |

### Validation Tool Results

| Tool | Result | Interpretation |
|---|---|---|
| `./gradlew :backend:test --tests "com.mastersmith.config.*"` | BUILD SUCCESSFUL。JUnit XML集計: 165件、失敗0・エラー0・スキップ0 | code-summary.mdの「165件成功」と一致。U5によるU2・U3・U4・U6・U7の変更後も、config-engineのテストは壊れていない。 |
| `./gradlew :backend:spotlessCheck :backend:checkstyleMain :backend:checkstyleTest` | BUILD SUCCESSFUL | フォーマット・Checkstyle違反なし。 |
| JaCoCo(jacocoTestReport.xmlの`com/mastersmith/config*`パッケージ集計) | 行カバレッジ 93.6%(未カバー33行・カバー484行、ほとんどがentityのgetter/setter) | code-summary.mdの93.6%と一致。フロア80%を満たす。 |
| source-manifestとの一致 | `git status`上、`backend/`配下に未コミット変更なし。`backend/src/main|test/java/com/mastersmith/config`の最終変更コミットはb0731bc(config-engineの完了時)から不変 | config-engineのソースは、U5の作業で意図せず変更・破壊されていない。manifest記載のパスが実在することも確認した(`backend/config/`・`gradle/`等)。 |
| 業務固有名のハードコード検査(config配下に`products/books/orders/customer/point`をgrep) | 該当なし | project.md Mandated(共通エンジン層に業務固有名をハードコードしない)は守られている。 |
| 起動時fail-fast | `ConfigEngineStartupRunner`(ApplicationRunner)が`cache.reload()`後に`validateAll`を実行。`ConfigEngineFailFastStartupTest`(実コンテキスト2フェーズ起動)が不正行でのコンテキスト起動失敗を確認 | BR1.1〜BR1.4の起動時fail-fastは実装・検証されている。 |

### Summary

config-engineのソース・テストはU5の作業後も無傷で(165件成功、カバレッジ93.6%、Spotless・Checkstyleも通過、manifestと一致)、起動時fail-fast・業務固有名の非ハードコード・型名の方言正規化・エンティティ単位のConfigChangedEvent発行は実装と検証が揃っている。一方、BR1.12(b)のSQL方言提供が未実装のままtraceabilityが「OK」・逸脱「なし」とされている点(R-01)と、importConfigSetにColumnConfigをID保持で上書きする経路がなく、BR1.14の「既存値の維持」も実装・検証されていない点(R-02)は、下流(list-engine/record-edit-engine/U9/U8)の実装を止めるかやり直させ得る設計上の欠落である。Criticalはなく、Majorは2件のためルール上はREADYとするが、承認者はR-01・R-02を認識して判断すべきである。R-03・R-04(監査ログの操作者・変更前後の値)は、開示済みの既知ギャップが、U5の完了後も未解消であることを示す。
