## Review

**Verdict:** NOT-READY
**Reviewer:** aidlc-architecture-reviewer-agent
**Date:** 2026-09-20T01:17:00Z
**Iteration:** 1

### Findings

| ID | Severity | Location | Finding | Required action | Status |
|---|---|---|---|---|---|
| R-01 | Major | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/user-management/nfr-design/reliability-design.md > NFR4.2 処理の流れ(手順1・2・6・9)と同時実行数の上限(5)。関連: performance-design.md > NFR1.4、logical-components.md > コンポーネント一覧(InvitationMailer・EmailLockRegistry) | 手順1「招待の同時実行数の許可を取る(満杯なら待たず503)」の実現手段が「InvitationMailExecutorのスレッド数5・待ち行列なしのプール」とされているが、performance-design.md NFR1.4ではこのプールが実行するのは手順6のメール送信だけである。プール満杯の判定(投入時の拒否)は手順6、つまりEmailLockRegistryの取得・トランザクション開始・DB書き込みの後になり、手順1で許可を取るという記述と実装手段が一致しない。この方式では、DB接続の枯渇を防ぐという上限5の目的が果たされず、リクエストスレッド(Tomcat)の数だけ手順3〜5でトランザクションを開けてしまう。別途セマフォを想定するなら、その部品がlogical-components.mdに存在しない。加えて、手順1の許可を取った後に手順2でEmailLockを最大12秒待つため、同一emailの待機者が許可を占有し、logical-components.mdの障害領域表にある「EmailLockRegistry障害時は他のemailの招待に影響なし」が成り立たない(同一emailへの5連打で全枠が埋まる)。さらに、手順7のコミット後に手順9で排他を返す順序は、排他の取得がトランザクション境界の外側にある場合にだけ成立する(@Transactionalなサービスメソッドの内側で排他を取ると、コミット前に排他が解放され、後続が未コミットの結果を見て直列化が崩れる)が、その境界(TransactionTemplate等)を規定していない。EmailLockRegistryの寿命管理(参照カウントで削除するか、ストライプロックか)も未規定で、ストライプ方式なら別emailどうしが直列化される。 | 招待の同時実行数の上限を、プールとは独立した明示的な許可(非ブロッキングのtryAcquire、手順1で取得、手順9で返却)として設計し、logical-components.mdに部品として追加する。許可・EmailLockの取得がトランザクションの外側にあることと、その実現方法(排他の取得と解放を、トランザクションを張る側の外側で行う構成)を明記する。同一emailの待機者が許可を占有する影響を、許可の取得順序(排他を先に取るか)または障害領域表の記述の修正で整合させる。EmailLockRegistryのキー管理方式を定める。 | New |
| R-02 | Major | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/user-management/nfr-design/performance-design.md > NFR1.3(ハッシュ更新を同じ許可の中で行う)。関連: reliability-design.md > NFR4.1(招待受諾の手順)、security-design.md > NFR2.2(ログイン成功時のハッシュ更新) | ハッシュ計算・検証・DB更新の順序と資源の取得順序が、設計として規定されていない。(1)招待受諾(認証不要)の手順は、NFR4.1ではトークン条件付きUPDATE(password_hashを含む)から始まり、UPDATEにはハッシュ済みの値が必要なため、ハッシュが先に計算される順序と読める。この場合、存在しないトークンでも、有効な長さのパスワードを付ければArgon2id(約19MiB・数百ms)を1回消費させられ、認証なしの攻撃者がHashConcurrencyLimiterの許可を使い切って、ログイン(C11)まで503にできる。functional-spec.md W2は「トークン検索(404)→検証→ハッシュ」の順である。(2)ログイン成功時のハッシュ更新(Q3=A)は、許可を保持したまま条件付きUPDATE(DB接続の取得を伴う)を行う。一方、受諾がトランザクション(接続保持)の内側でハッシュの許可を待つ実装になると、接続を持つ側が許可を待ち、許可を持つ側が接続を待つ循環待ちになり、2秒の待機上限が解消するまで両者が503になる(自己解消はするが、上限5の招待が接続を占有している間は起きやすい)。(3)C11の`verifyPasswordHash`は、呼び出し元(U5)が読み取り専用トランザクションで呼ぶ場合、書き込みが失敗して警告ログだけが残り、更新が永久に行われない。この副作用(書き込み)はC11の契約追補(保留一覧)にも載っていない。 | 招待受諾の順序を「トークンの存在確認(トランザクション外の軽い検索)→パスワード長の検証→許可を取ってハッシュ計算→許可を返却→トランザクションを開始して条件付きUPDATE」と規定し、未知トークンではハッシュを計算しないことを明記する。ハッシュ更新は、許可を返してから別のトランザクション(REQUIRES_NEWまたはTransactionTemplate)で条件付きUPDATEを行うか、許可の中で行う場合は接続の取得順序(許可を先、接続を後)を全経路で統一することを明記する。C11のハッシュ更新が書き込みを伴うことと、呼び出し元のトランザクション属性に依存しない実装方法を、契約追補の保留一覧に追加する。 | New |
| R-03 | Major | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/user-management/nfr-design/reliability-design.md > NFR4.3(コミット後の発行と同期@EventListener)。関連: logical-components.md > 外部との境界(AuditLogging) | UserChangedEventを「トランザクション同期のafterCommit」で発行し、受信側は「他ユニットと同じ、同期の@EventListenerとtry-catch」で受けるとしているが、これでは監査ログの書き込みが確定しない恐れがある。Springのafter-commitコールバックの中では、元のトランザクションはすでにコミット済みで、リソースは束縛されたままである。受信側(audit-logging)がREQUIREDのトランザクションで監査ログをINSERTすると、元の(コミット済み)トランザクションに参加するだけで、以降コミットされず、書き込みが失われる。受信側はtry-catchで例外を握りつぶす(fire-and-forget)ため、失われたことに誰も気づかない。監査ログはproject.md Mandatedの必須事項(変更前後の値の記録)であり、「他ユニットと同じ」方式は、他ユニットが変更のトランザクションの内側で同期発行しているのであれば、コミット後に発行する本設計では前提が変わる。共有契約(contract-summary.md・components.md)には受信側のトランザクション属性の定めがなく、この点は契約でも解決されていない。加えて、afterCommit内で発行側が例外を受けた場合、Springはその例外を呼び出し元へ伝播するため、コミット済みかつメール送信済みの招待がHTTP 500で返る。発行側のtry-catchも規定されていない。 | 発行側の設計に、(a)発行を、受信側が独立したトランザクション(REQUIRES_NEW)で書き込むことを前提とする、または、発行側でTransactionTemplate(REQUIRES_NEW)の中で発行する、のいずれかを明記し、audit-logging側に必要な契約(トランザクション属性の定め)を保留の契約追補として記録する。(b)発行側でも例外を捕捉し、警告ログ(userIdのみ)とメトリクスに記録して、HTTPの結果に影響させない。(c)テスト設計(NFR8.2)に、コミット後の発行で監査ログの行が実際に永続化されることの統合テストを追加する。 | New |
| R-04 | Major | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/user-management/nfr-design/logical-components.md > コンポーネント一覧(UserController・ApiExceptionHandler・RequestLogFilter・RequestSizeLimitFilter)、外部との境界。関連: security-design.md > NFR2.9・NFR2.10、observability-design.md > NFR5.3 | アプリケーション全体に効く横断的な部品を、user-managementの部品として定義しており、所有者と他ユニットとの境界が不明確である。(1)`ApiExceptionHandler`(@ControllerAdvice)を「すべてのエラーをRFC 9457に変換」する部品とし、security-design.md NFR2.9とperformance-design.md NFR1.3は「共通の例外ハンドラ」と呼ぶ。共通(全ユニット)であれば、U4固有の`HashCapacityExceededException`(503)への依存が共通側に生じ、依存方向が逆転する。U4固有であれば、対象パッケージの限定がなく、他ユニットの例外処理と二重定義・順序の衝突を起こす。(2)トレース属性の差し替え(`ServerRequestObservationConvention`)は、アプリケーションに1つの設定であり、全ユニットのHTTPスパン・メトリクスに影響する。(3)`Referrer-Policy: no-referrer`を「Spring Securityの設定で全レスポンスに付与」するのは、認証フィルタチェーン(U5の領域と推定される)の設定である。NFR Requirementsのレビュー指摘(Referrer-Policyの所有者)は、このため閉じていない。(4)`RequestLogFilter`と`RequestSizeLimitFilter`も、全体のフィルタチェーンでの順序と適用範囲(URLパターン)の所有者が未定である。 | 各横断部品について、(a)共通基盤が所有するもの(例外ハンドラの基底、観測の規約、セキュリティヘッダー、リクエストログ)と、(b)U4が所有するもの(U4固有の例外の型とその変換の登録、対象パスに限定したRequestSizeLimitFilter)を分けて記載する。共通基盤が所有するものは、U4が要求する事項(ルートのテンプレートの使用、U4の例外の503への写像、Referrer-Policyの付与)を、他ユニットへの要求として保留の契約追補または未解決事項に記録する。U4の例外変換は、対象パッケージを限定した@ControllerAdviceとし、@Orderを明記する。 | New |
| R-05 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/user-management/nfr-design/security-design.md > NFR2.10(招待トークンのURL露出の抑止)。関連: observability-design.md > NFR5.3、nfr-requirements/tech-stack-decisions.md(メトリクス・トレーシングのライブラリ未確定) | トークンのマスクについて、次の実装上の穴と未検証の前提がある。(1)`RequestLogFilter`が「マッチしたルートのテンプレート」をパスの記録に使うとあるが、ルートのテンプレートは、リクエストがハンドラのマッピングを通った後にしか分からない。フィルタの入口で記録する場合は使えず、処理後に記録する場合、413(RequestSizeLimitFilter内の拒否)や、ハンドラ到達前に失敗した場合は、テンプレートが得られず、生のパスが記録される恐れがある。フォールバックの規定がない。(2)`org.springframework.web`のDispatcherServletのDEBUGログや、Tomcatの内部ログなど、フレームワークの標準のロガーは、生のリクエストのパスを出しうる。これらのログレベルの制約が設計にない。(3)`ServerRequestObservationConvention`の差し替えは、Micrometer ObservationとMicrometer Tracingのブリッジを使う場合にだけ有効である。tech-stack-decisions.mdは「OTEL基盤へエクスポート(具体的なライブラリ選定はCI Pipelineで確定)」としており、OTELのJavaエージェントを使う場合はスパンのURL属性を差し替える別の手段が必要になる。この前提が明記されていない。(4)Spring MVCが返すProblemDetailは、`instance`に既定でリクエストのパスを入れるため、受諾APIのエラー応答に、トークン入りのパスが入る(要求者本人が知る値だが、フロントエンドのエラー報告に流れうる)。 | (1)ルートのテンプレートが得られない場合のフォールバック(パスを固定文字列に置換、または受諾APIのパスパターンに一致する場合は常にテンプレート表記へ置換)を規定する。(2)フレームワークのロガーのレベルの制約(本番でDEBUG以下を使わない、または該当ロガーの抑止)を設計に加える。(3)トレースの実装(Micrometer Tracingか、OTELエージェントか)が未確定であることを明記し、確定するまでの前提と、確定後の検証(NFR8.2のテスト)を紐づける。(4)ProblemDetailsの`instance`をトークンを含まない値(ルートのテンプレート等)にする。 | New |
| R-06 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/user-management/nfr-design/security-design.md > NFR2.10(リンクは`<ベースURL>/invitations/accept#token=<トークン>`、受諾画面は「U12の責務」) | 招待リンクの形式(ルート`/invitations/accept`とフラグメントのキー`token`)は、招待メールを生成するU4と、受諾画面を提供するU12の間のインタフェースであるが、C5にも他の共有契約にも存在せず、nfr-requirements/tech-stack-decisions.mdの「契約追補(保留事項の一覧)」7項目にも含まれない。「U12の責務」(フラグメントの読み取り、`history.replaceState`による消去、トークンを画面状態・ログ・外部送信に含めないこと、受諾画面のルートが認証不要で開けること)は本設計の中で断定されているだけで、保留事項としての記録がない。 | 契約追補の保留一覧に、リンクの形式(ルート・フラグメントのキー)と、U12への要求事項(replaceState、トークン非露出、認証なしで開けるルート、SPAのフォールバック)を8番として追加する。 | New |
| R-07 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/user-management/nfr-design/performance-design.md > NFR1.4(SMTPのタイムアウト「接続3秒・読み取り5秒・書き込み2秒(合計10秒)」、呼び出し側の10秒の打ち切り) | Jakarta Mail(spring-boot-starter-mail)のタイムアウトは、接続・読み取り・書き込みのそれぞれ1操作ごとの上限であり、合計10秒を保証しない。SMTPの会話は複数のコマンド往復(EHLO・STARTTLS・AUTH・MAIL FROM・RCPT TO・DATA等)を含むため、各応答が5秒近くかかる遅延では、1回の送信が数十秒に及びうる。呼び出し側が10秒で待つのをやめても、ブロッキングI/O中の送信スレッドは割り込みで止まらず、プール(5スレッド・待ち行列なし)を占有し続けるため、SMTP遅延の間、10秒経過後も新規の招待が投入時に503で拒否される時間が延びる。また、打ち切り後に送信が完了すると、ロールバック済みで存在しないトークンのリンクを含むメールが届く。設計はこれらを述べていない。 | 「合計10秒」を、呼び出し側の打ち切りによる上限であると訂正し、各タイムアウトが操作ごとであることを明記する。打ち切り後にスレッドが残る挙動(プールの占有、ロールバック後にメールが届きうること、届いたリンクは404になること)を、制約または残余リスクとして記録する。 | New |
| R-08 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/user-management/nfr-design/reliability-design.md > NFR4.1(PUTのbeforeValue)、security-design.md > NFR2.7(件名の拒否を503)。関連: logical-components.md > 障害領域と影響の範囲、共有するリソース | 前段のNFR Requirementsレビューの持ち越し事項に対する対応の状況: (a)beforeValueの並行性(前段R-11)は閉じていない。設計は「同時更新でbeforeValueが正確でない場合がありうることを制約として受け入れる」とするが、変更前後の値の記録はproject.md Mandated(監査ログ)の中核であり、対象行のSELECT FOR UPDATE(行ロック)を同じトランザクションで取れば、後勝ちの方針を変えずに正確なbeforeValueが得られる。(b)氏名・件名の長さや改行による拒否が503になる問題(前段R-15)は閉じていない。設計は件名の拒否を「招待全体を巻き戻して503」とし、BR4.15のname検証の追補(保留の7番)にも言及しない。nameに最大長の定めがないため、200文字近い氏名では、管理者の入力誤りが再試行不能な503として返り、SMTP不調用のカウンタとアラート(5分間に3回)を誤って作動させる。(c)再招待の条件付き更新・ロック待ち(前段R-14)は、statusの条件と422・503の変換が規定され、ほぼ閉じている。(d)i18nの出典(前段R-17)は[assumption]として記録済み。(e)障害領域の表は「SMTP障害は一覧・更新・無効化・受諾・ログインに影響しない」とするが、同じ文書の「共有するリソース」が、内部設定DBの接続プールを他ユニットと共有し、招待が最大5接続を最大約12秒保持すると述べており、プールの大きさの規定もないため、この主張は成り立たない。 | (a)更新・無効化の対象行を、トランザクション内で行ロック付きで読むことを設計に加える。(b)件名・氏名の入力起因の拒否を、503ではなく422(フィールド単位)にするために、BR4.15の追補(nameの最大長・制御文字の禁止)を設計に反映し、テンプレート起因の拒否のみを503とする。(c)障害領域の表を、接続プールの共有を踏まえた記述に直し、内部設定DBの接続プールの最小サイズ(招待の上限5に、通常の同時処理数を加えた値)を規定する。 | New |
| R-09 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/user-management/nfr-design/security-design.md > 残余リスク(設計上の追記)、NFR2.7、NFR2.5。関連: nfr-requirements/security-requirements.md > 残余リスク3・NFR2.7 | 前段が「見直し先: NFR Design」と指定した事項の処理が不足している。(1)残余リスク3(招待トークンの平文・無期限保存)は、見直し先がNFR Designであるにもかかわらず、本設計は「残余リスク(1〜5)に加えて」と件数を引き継ぐだけで、受容の再確認も、トークンのハッシュ保存の採否も記録していない。(2)残余リスク6は、暗号化なしの誤設定で平文になるのが招待リンクだけと読めるが、SMTPの認証情報(SMTP AUTH)も同様に平文で流れる。(3)Q2=Bの採用で、前段のNFR2.7の[assumption](本番相当では暗号化必須)は上書きされるが、前段の記述はそのまま残っており、参照する開発者は矛盾を見る。(4)ベースURLの「本番相当ではHTTPSを必須とし起動時に失敗」は、アプリが「本番相当」をどう判定するか(プロファイル等)が未定義で、暗号化の検査を行わないとしたQ2=Bの考え方と整合させる必要がある。(5)無効化(disabled)に加え、PUTでroleIdsが外されたユーザーも、既発行トークンのactiveRoleIdによって最大10分は旧ロールの権限で操作できる(残余リスク1の対象外)。 | (1)残余リスク3の受容(または採用)を、理由とともに記録する。(2)残余リスク6にSMTP認証情報を追記する。(3)security-design.mdにNFR2.7の上書き(Q2=B)を、前段のどの記述を置き換えるかとともに明記する。(4)「本番相当」の判定方法(設定値・プロファイル)を1文で規定する。(5)残余リスク1に、ロール変更後も既発行トークンが有効な間は旧ロールの権限が通ることを追記する。 | New |
| R-10 | Minor | aidlc/spaces/default/intents/260910-master-mgmt-app/construction/user-management/nfr-design/security-design.md > NFR2.3(RequestSizeLimitFilter)、NFR2.7(MailTemplateRenderer・起動時検証)。関連: logical-components.md > NFR8.2 | 詳細設計として不足している点。(1)チャンク転送のボディは、フィルタでは内容を処理する前に拒否できず、リクエストの入力ストリームを包んで読み込み量を数え、64KiBを超えた時点で例外を投げる方式になる。この例外は、Jacksonのデシリアライズ中にHttpMessageNotReadableException(400)へ包まれ、413へ変換する経路(ApiExceptionHandlerでの原因の判別)が規定されていない。フィルタの位置が認証フィルタの後(構成図)であることの影響(認証前の巨大ボディ)も述べていない。(2)テンプレートの`<title>`の欠落・空をfail fastで検知する起動時検証(tech-stack-decisionsの確認事項5)は、NFR8.2のテスト表に現れるが、どの部品がいつ検査するかが本設計にない。ja・enの両言語のテンプレートが検査対象であることも未記載。(3)メトリクスの名前は、要件が付けた形(`_total`・`_seconds`)がPrometheusの命名規約であり、OTLPで出力する場合は名前がドット区切りのまま(`_total`なし)になるため、「エクスポート時に要件の名前に対応する」と述べるだけでは、アラートの条件の名前が確定しない。(4)NFR5.4は、内部設定DBのヘルスチェックに「参加する」と述べるだけで、対応する具体的な要素(HealthIndicator等)が示されていない。 | (1)413への変換の経路(カウンタ付きの入力ストリームと、原因の例外の判別)を1〜2文で規定する。(2)`<title>`の起動時検証の担当部品と、対象(全言語のテンプレート)を規定する。(3)メトリクスのレジストリ(Prometheus・OTLP)と名前の対応を決める。(4)NFR5.4のヘルスチェックの実現方法(共通のDataSourceヘルスに含まれることの明記)を記載する。 | New |

### Validation Tool Results

| Tool | Result | Interpretation |
|---|---|---|
| traceability.json の構文と網羅(自前の確認) | PASS: JSONとして解析でき、upstream_idsとcoverageはともに33件で一致。nfr-requirementsの各成果物の見出しにあるNFR ID(33件)と過不足なく一致 | NFR1.1〜1.4・2.1〜2.11・3.1〜3.4・4.1〜4.5・5.1〜5.5・7.1〜7.2・8.1〜8.2の全IDが網羅されている |
| traceability.json の target の存在(自前の確認) | PASS: 33件すべての target に書かれたファイルが存在し、該当するNFRの見出しが、そのファイルの中に存在する | 参照は解決する。ただし、NFR3.1・3.2・3.4・4.5・5.4・5.5・2.8は要件の言い換えに近い記述で、具体的な設計は薄い(R-08・R-10)。ステータスがすべてOKであることは、設計の充実度を保証しない |
| NFR Design Q1〜Q3との整合(手作業) | PASS(注記あり): Q1=Aのフラグメント、Q2=Bの暗号化検査なし+残余リスク6、Q3=Aのハッシュ更新は、いずれも設計に反映されている | Q1〜Q3に矛盾はない。ただしQ3のハッシュ更新は、R-02(順序・トランザクション・契約追補)の課題が残る。Q2=Bは前段のNFR2.7との整合の記述が不足(R-09) |

### Summary

前段の要件を、部品・数値・順序に落とした設計として一定の完成度があり、Q1〜Q3の確定回答とも矛盾はないが、リソースの取得順序と境界(招待の同時実行数の許可、ハッシュの許可とDB接続、コミット後の監査イベントの書き込み、横断部品の所有者)に、実装者が推測で埋めるしかない欠落が4件あり、NOT-READYとする。特に、after-commit発行と同期リスナーの組み合わせで監査ログの書き込みが確定しない恐れ(R-03)と、認証不要の受諾APIでのハッシュ計算の順序(R-02)は、修正が必要である。
