## Review

**Verdict:** READY
**Reviewer:** aidlc-architecture-reviewer-agent
**Date:** 2026-09-20T00:22:25Z
**Iteration:** 2

### Findings

パスの略記: `FD` = `aidlc/spaces/default/intents/260910-master-mgmt-app/construction/user-management/functional-design`、`CS` = `aidlc/spaces/default/intents/260910-master-mgmt-app/inception/contract-design/contract-summary.md`。R-01 から R-12 は iteration 1 の指摘であり、ID を維持して再判定した。R-13 以降が今回の新規指摘である。

| ID | Severity | Location | Finding | Required action | Status |
|---|---|---|---|---|---|
| R-01 | Major | FD/rules.md > BR4.12, BR4.10; FD/functional-spec.md > Assumptions(BR4.12 の項) | ロール付与による権限昇格の防止が BR4.12 として追加された。roleIds の付与・変更を canAccessScreen で user-management が true となる操作者に限り、自分自身の roleIds 変更を 422 で禁止している。「権限管理者」の解釈と、より厳格な「自分の実効権限を超えるロールは付与できない」判定を設計していない点は [assumption] として正直に記録されており、project.md Forbidden の文言(権限管理者による明示的な操作を経ない昇格の禁止)とも矛盾しない。 | なし。人間が承認ゲートで assumption を確認する。 | Resolved |
| R-02 | Major | FD/functional-spec.md > 認可の前提, W6; FD/rules.md > BR4.8, BR4.10, BR4.9 | 依存方向は U5 から U4 のままで DAG を壊さない旨が明記された。BR4.8 は userId のみで 401/成功を判定し activeRoleId 未選択でも動作する。BR4.9 の actor の出所も W1 から W5 で定義された。取得経路の具体化(トークンのクレーム)に残る問題は新規指摘 R-13 として切り出す。 | なし(残課題は R-13 で追跡)。 | Resolved |
| R-03 | Major | FD/rules.md > BR4.1, BR4.11; FD/functional-spec.md > W1 手順4, W4 手順5, 状態遷移図; FD/entities.md > invitationToken | 再招待(invited への同一 email の再 POST でトークン再発行)と取消(invited への DELETE で disabled 化とトークン null 化)が設計され、BR4.1 の重複判定の扱いも明記された。Q2 の前提「無効化して再招待」との食い違いは assumption として訂正済み。disabled ユーザーの復帰が対象外である点も記録されている。 | なし。 | Resolved |
| R-04 | Minor | FD/functional-spec.md > W8 手順4, Assumptions(revokeRefreshTokensOnDisable); FD/traceability.json > FR2.3 | 部分的に解決済み(大半は解消、残りは範囲が狭まったため Major から Minor へ引き下げ)。W8 と BR4.13 で findByEmail(Group 経由ロールとの和集合、passwordHash は null)、verifyPasswordHash、isDisabled(キャッシュなし・fail closed)の提供側の振る舞いが定義された。残るのは次の 2 点。(a) C11 の revokeRefreshTokensOnDisable は U4 が提供するインタフェースのメソッドであり、U4 の実装が何をするか(no-op か、FR2.3 の再認証拒否は isDisabled で担保する旨のドキュメント化された空実装か)が未定義で、Code Generation が推測を要する。(b) traceability.json の FR2.3 は OK のまま。注記に honest な但し書きはあるが、即時失効の機構は U5 の未確定事項に依存する。 | W8 手順4 に U4 側の実装方針(例: 空実装とし、無効化の即時反映は isDisabled の再認証時確認で担保する)を 1 行で明記する。FR2.3 の traceability の注記に、未確定部分は U5 依存である旨を残す。 | Unresolved |
| R-05 | Major | FD/entities.md > UserChangedEvent; FD/rules.md > BR4.9; FD/functional-spec.md > W1 手順8, W2 手順6, W5 手順4 | operation に ACTIVATED と BOOTSTRAPPED が追加され、W2 は 2 件(INVITED と ACTIVATED)に統一された。BR4.9、entities.md、functional-spec.md の記述は一致している。targetType は固定属性 User として定義され、actor は W2 で受諾者本人、W5 で system と定義された。W5 のイベント発行も定義された。system や新 operation 値を audit-logging がどう受けるかは R-16 で追跡する。 | なし(残課題は R-16)。 | Resolved |
| R-06 | Major | FD/functional-spec.md > W5; FD/rules.md > BR4.7, BR4.3; FD/entities.md > User.roleIds | 初期管理者の roleIds を application.yml の任意項目 initial-admin.role-ids とし BR4.5 の実在検証を除外する方針、パスワード長の fail fast、UserPreference の既定値作成、既存 User(disabled を含む)がある場合の冪等・復旧対象外の扱いが定義された。誤参照だった BR3.13 の記述はなくなった。残課題は name の出所(R-14)と、到達性の主張に対する未確定(R-17)。 | なし(残課題は R-14 と R-17)。 | Resolved |
| R-07 | Major | FD/functional-spec.md > W7, W3, Assumptions(Contract Design 追補の項); FD/rules.md > BR4.14; CS > C5 | GET /api/users の workflow(W7)と出力項目(機微項目の除外、email 昇順、ページングなし)、PUT の更新可能項目(name と roleIds のみ、他は 422)が追加された。C5 に必要な追補(401・404・422・503、PUT の requestBody、受諾の任意項目)は open question として列挙された。 | なし。 | Resolved |
| R-08 | Minor | FD/rules.md > BR4.6; FD/functional-spec.md > W4 手順4, 状態遷移図の注記 | 自己無効化の禁止(422)、disabled への再 DELETE の冪等な 204(イベント重複なし)、復帰手段が対象外である旨が追加された。最後の有効な管理者の保護をしない点は assumption として記録されている。 | なし。 | Resolved |
| R-09 | Minor | FD/rules.md > BR4.1, BR4.11, BR4.2; FD/functional-spec.md > W1 手順4 から 6 | 部分的に解決済み。大半は解決済みで、受諾時のトークン null 化、同一トークンの並行受諾の条件付き更新、URL パス中トークンのマスク、パスワードの 8 から 128 文字(コードポイント数)、使用済みトークンの 404 統一、Argon2id パラメータの NFR 委譲の記録が入った。残るのは招待側の競合である。(a) 同時 POST /api/users で同一 email が競合した場合の一意制約違反を 422 へ変換する指示がない。(b) 再招待(W1 手順6)が status=invited を条件とする原子的更新として書かれていないため、並行受諾と衝突すると active になった User へ新トークンと name/roleIds を書き込みうる(entities.md の invitationToken は invited の間のみ非 null という制約に反する)。残課題は新規指摘 R-15 に集約して追跡する。 | R-15 で対応する。 | Unresolved |
| R-10 | Minor | FD/functional-spec.md > エンティティ関連図, 状態遷移; FD/entities.md > User.entity_constraints, relationships | ER 図とカーディナリティが 0 または 1 に修正され、email の一意制約の誤記も直った。2 つの mermaid 図の両方にテキストフォールバックがある。rules.md と entities.md の YAML は構文上問題なく読み込めた(16 ルール、3 エンティティ)。 | なし。 | Resolved |
| R-11 | Minor | FD/traceability.json > upstream_ids, coverage | FR1.6 が upstream_ids と coverage(BR4.14)に追加された。FR2.8 は専用の BR4.16 へ割り当てられ、誤った手順番号の注記もなくなった。FR2.3 の status の実質性は R-04 で追跡する。 | なし。 | Resolved |
| R-12 | Minor | FD/rules.md > BR4.15, BR4.10; FD/functional-spec.md > Assumptions(ダングリング参照の項) | email の trim・小文字化と形式検証、name の必須検証(BR4.15)が追加された。screenKey user-management は permission-engine の予約キーである旨が BR4.10 に明記され、ロール削除時の User.roleIds の扱いは open question に追加された。予約キーの実在は渡された共有契約(C10)からは確認できないため、permission-engine 側の設計で確認が必要である(read-scope のため sibling は開いていない)。 | 承認ゲートで、user-management の screenKey が permission-engine 側で予約されていることを確認する。 | Resolved |
| R-13 | Minor | FD/functional-spec.md > 認可の前提, Assumptions(トークンのクレームの項); FD/rules.md > BR4.8, BR4.10; CS > C4 の active-role, C14 | 操作者の userId と activeRoleId を「トークンのクレーム」から得るとしているが、C4 の PUT /api/auth/active-role は新しいトークンを返さず「以降のセッションで保持」と記し、C14 の getActiveRoleId は sessionId を引数に取るため、activeRoleId は実際にはセッション側の状態である可能性が高い。list-engine と record-edit-engine が C14 を使う一方で U4 だけが別経路(セキュリティコンテキスト)を使うと、activeRoleId の取得元が二重になる。認証フィルタが設定するセキュリティコンテキストという暗黙の契約は契約一覧のどこにもなく、open question に「クレームに含まれること」としか記載がない。クレームか C14 相当のセッション参照かによらず U4 は同じ抽象(現在の操作者)を読むだけで済むため、記述をそこまで一般化するのが安全である。矛盾ではなく記述の精度の問題であり、assumption として記録済みのため Minor とする。 | 「トークンのクレーム」という具体化をやめ、認証フィルタが設定する現在の操作者(userId と activeRoleId)を読む、と抽象化して記述する。Contract Design 追補の open question に、この認証コンテキストの提供が U5 の責務であることを追記する。 | New |
| R-14 | Minor | FD/functional-spec.md > W5 手順2; FD/rules.md > BR4.7; FD/entities.md > User.name | User.name は required(必須プロパティ)だが、W5 と BR4.7 は初期管理者の name をどう決めるかを述べていない(FR2.4 が定めるのは email とパスワードのみ)。BOOTSTRAPPED イベントの afterValue にも name を含める。Code Generation が name の値を推測する必要がある。 | 初期管理者の name の決め方(例: email を用いる、または任意項目 initial-admin.name を追加して未指定時は email)を BR4.7 と W5 に明記するか、assumption として記録する。 | New |
| R-15 | Minor | FD/functional-spec.md > W1 手順4, 6; FD/rules.md > BR4.1, BR4.11 | R-09 の残課題。(a) 同一 email の同時 POST /api/users で一意制約違反が起きた場合の応答(422 へ変換するのか)が未定義。(b) 再招待が「status=invited の条件付き更新」として書かれておらず、並行する招待受諾(W2)と競合すると、active になった User に新しい invitationToken と name/roleIds を書き込むおそれがある。BR4.2 では受諾側のみ原子的にしてある。(c) W1 手順7 の「巻き戻し」は、DB 更新とメール送信の順序(送信をコミット前に行うのか)が未定義。送信済みメールのトークンが巻き戻しで無効になる場合がある。 | W1 に、一意制約違反を 422 へ変換すること、再招待を status=invited を条件とする原子的更新とすること(条件を満たさなければ 422 または 404)、送信とコミットの順序方針を 1 から 2 文で追記する。 | New |
| R-16 | Minor | FD/entities.md > UserChangedEvent.actor; FD/rules.md > BR4.9; CS > C6 AuditLogEntry | BOOTSTRAPPED の actor を固定文字列 system とし「AuditLogging の actorRaw に相当する扱いを想定」と記すが、C6 の AuditLogEntry は actorUserId のみで actorRaw を持たず、components.md も actorUserId を User への参照として記す。actorRaw という語は共有契約から確認できず(audit-logging の設計を開いていないため未検証)、system や新 operation 値(ACTIVATED、BOOTSTRAPPED)を audit-logging がどの列にどう格納するかは U7 側の受け入れ確認が必要である。これは契約追補の open question に載っていない。 | 「actorRaw に相当する」という断定を「audit-logging が actor を userId 以外の値でも受理する必要がある」と言い換え、C6 側の扱い(actorUserId に system を許すか)と operationType の値の受け入れを Contract Design 追補または U7 への確認事項として open question に追加する。 | New |
| R-17 | Minor | FD/functional-spec.md > W5 末尾の段落, Assumptions(初期管理者の roleIds の項) | W5 は「permission-engine のブートストラップ例外により、初期管理者は最初の RBAC 設定インポート画面へ到達できる」と断定しているが、(a) この例外は渡された共有契約(CS の C10 と C7)にも components.md にも記載がなく、共有契約からは検証できない、(b) 既定では initial-admin.role-ids が空になり、ロールを持たないユーザーはログイン後にアクティブロールを選択できず、例外を使うための canAccessScreen の呼び出し自体が成立しない可能性がある。後者は open question に記載があるが、W5 の断定文と矛盾して読める。 | W5 の断定文を「permission-engine のブートストラップ例外(設計上の前提。共有契約では未確認)が成立し、かつ初期管理者がアクティブロールを得られる場合に到達できる」という条件付きの記述に改める。initial-admin.role-ids を実運用で必須にするか、空の場合の到達手順を U5 の機能設計で確定するかを承認ゲートで人間に確認する。 | New |
| R-18 | Minor | FD/functional-spec.md > W3 手順4 から 7; FD/rules.md > BR4.12, BR4.14, BR4.9 | PUT /api/users/{userId} の細部が未定義。(a) roleIds を省略した場合に現状維持か空へ置換かが不明(BR4.5 は「指定・変更する場合」と書き、BR4.12 は「現在値と異なる場合」と書くだけ)。(b) roleIds の比較を順序込みのリストとするか集合とするかが不明で、自己変更判定の結果が変わる。(c) 変更がない PUT で UPDATED イベントを発行するか(BR4.9 は冪等な再実行では発行しないと述べるが PUT には触れない)。(d) invited や disabled の対象への PUT の可否。 | BR4.14 または W3 に、roleIds 省略時の意味、roleIds の比較は集合としてよいこと、変更なしの PUT ではイベントを発行しないこと、invited と disabled の対象の扱いを追記する。 | New |
| R-19 | Minor | FD/rules.md > BR4.15 trigger, BR4.7 logic, BR4.10 statement; FD/functional-spec.md > W5 手順1, W2 | 軽微な記述の不一致。(a) BR4.15 の trigger は「招待受諾の email・name 入力時」とするが、招待受諾のリクエストに email はない。(b) W5 手順1 は email 形式不正と SMTP 設定不備の fail fast を挙げるが、BR4.7 の statement と logic はパスワード長のみで、対応する BR4.16 は SMTP 不備しか述べない。(c) BR4.10 の適用範囲「/api/users系」は、認証不要の POST /api/users/invitations/{token}/accept を含んで読めるが、W2 は認証不要としている。 | BR4.15 の trigger から受諾時の email を外す。BR4.7 に email 形式不正の fail fast を加える。BR4.10 の適用範囲を「受諾エンドポイントを除く」と明記する。 | New |

### Validation Tool Results

| Tool | Result | Interpretation |
|---|---|---|
| traceability(手動確認、advisory) | PASS。traceability.json が挙げる BRx.y はすべて rules.md に存在する。rules.md の 16 ルール(BR4.1 から BR4.16)はすべて coverage または reverse(BR4.9 と BR4.10 は N/A)で扱われ、孤児なし。upstream_ids(FR1.6, FR2.1 から FR2.6, FR2.8, FR9.1, FR10.1)は unit-of-work-story-map.md が U4 に割り当てた FR を網羅し、FR10.1 は unit-of-work.md の U4 責務(ユーザー単位の言語切替手段)にも根拠がある | 構造上の欠落なし。FR2.3 の OK の実質性のみ R-04 |
| YAML と JSON の構文検査 | PASS。rules.md と entities.md の yaml ブロックは読み込みに成功し、traceability.json も有効な JSON | 機械可読性に問題なし |
| 内部整合(entities.md、rules.md、functional-spec.md、traceability.json) | BR4.9 の operation 値・W2 のイベント件数・actor の定義、User と UserPreference のカーディナリティ、BR 参照(W1 から W8 の BR 番号)はおおむね一致している。矛盾は R-14、R-15、R-18、R-19 の軽微な欠落・不一致のみ | Critical なし |
| 契約整合(CS: C4、C5、C6、C10、C11、C14、components.md) | C5 の追補内容は open question に列挙済み。C11 の UserAccount.passwordHash を null とする方針も追補案 (5) に含まれる。C4 と C14 に対する操作者取得経路の記述は R-13、C6 への system actor は R-16、permission-engine のブートストラップ例外と screenKey 予約は共有契約から検証できず R-12 と R-17 | いずれも Minor。Code Generation を止める欠落ではない |

### Summary

iteration 1 の Major 7 件のうち 6 件は解決し、R-04 は範囲が狭まった Minor として残る(R-09 も Minor で、残課題は R-15 に集約)。新しい規則群(BR4.11 から BR4.16、W7、W8、ACTIVATED と BOOTSTRAPPED のイベント)はエンティティ、規則、ワークフロー、トレーサビリティの間で概ね整合し、project.md の Mandated と Forbidden にも反していない。新規の指摘は Critical と Major を含まない 7 件の Minor(R-13 から R-19)で、主なものは操作者取得経路の記述の精度(R-13)、初期管理者の name(R-14)、招待側の競合(R-15)である。READY 基準(Critical なし、Major が 2 件以下)を満たすため READY とする。Minor は承認ゲートで人間に見せ、必要なら Request Changes で取り込む。
