## Review

**Verdict:** NOT-READY
**Reviewer:** aidlc-architecture-reviewer-agent
**Date:** 2026-09-20T00:06:18Z
**Iteration:** 1

### Findings

パスの略記: `FD` = `aidlc/spaces/default/intents/260910-master-mgmt-app/construction/user-management/functional-design`、`CS` = `aidlc/spaces/default/intents/260910-master-mgmt-app/inception/contract-design/contract-summary.md`。

| ID | Severity | Location | Finding | Required action | Status |
|---|---|---|---|---|---|
| R-01 | Major | FD/rules.md > BR4.5, BR4.10; FD/functional-spec.md > W1 手順4, W3 手順4 | project.md `## Forbidden`「権限の昇格(自分自身への昇格を含む)を、権限管理者による明示的な操作を経ずに許可しない」に対応するルールが本ユニットに存在しない。`POST /api/users` と `PUT /api/users/{userId}` は、`canAccessScreen(activeRoleId, "user-management")` が true なら任意の `roleIds` を付与できる。BR4.5 は roleId の「実在」しか検証しない。したがって管理者が自分自身へ、または自分の実効権限を超えるロール(例: 最上位の管理ロール)を付与できてしまう。C10 の `assignPermission` / `assignAuxiliaryPermission` は `actorRoleId` による昇格防止(BR3.8)を持つが、ロール割当(User.roleIds)側には同等の判定が無く、この空白は Assumptions & Open Questions にも記録されていない。 | User.roleIds の付与・変更に対する昇格防止ルール(例: 操作者は自身の実効権限を超えるロールを付与できない、自分自身の roleIds は変更できない、等)を BR として追加するか、権限管理者のみに限定する方針を明示する。C10 に判定メソッドが必要ならBR4.5と同様に契約追補の open question として記録する。 | New |
| R-02 | Major | FD/rules.md > BR4.8, BR4.10; FD/functional-spec.md > W6 手順2, W1 手順2; CS > C14 consumers, components.md 依存グラフ(AuthenticationService から UserManagement への依存) | 認可の前提となる `activeRoleId` の取得手段が契約上どこにも定義されていない。BR4.8 は `ActiveRoleResolver` を参照するが、この名称は渡された契約(C10/C11/C14)に存在しない。C14 `SessionContextApi.getActiveRoleId` の consumers は list-engine と record-edit-engine のみで user-management は含まれない。かつ AuthenticationService は UserManagement へ sync 依存(C11)しているため、user-management から authentication-service への同期呼び出しを追加すると U4 と U5 の循環依存になる。また BR4.8 は「activeRoleId を解決できる=認証済み」と等置しているが、FR4.2 により複数ロールのユーザーはロール選択前は activeRoleId が未解決であり、その状態では `/api/me/preferences` が 401 になる(FR9.1 が求める全画面へのテーマ適用がロール選択画面で成立しない)。さらに BR4.9 の `actor`(操作者 userId)をどこから得るかも W1/W3/W4 に書かれていない。 | activeRoleId と操作者 userId の取得経路(Bearer トークンのクレームから取得するか、C14 consumers への user-management 追加か等)を決め、依存方向が U5 から U4 への向きのままで DAG を壊さないことを明記する。BR4.8 の認可条件は「認証済み(principal の userId が取得できる)」に改め、activeRoleId 未選択でも動作するようにする。契約変更が要るなら open question に追加する。 | New |
| R-03 | Major | FD/functional-spec.md > 状態遷移(User.status)の注記, W4; FD/rules.md > BR4.1, BR4.2; FD/entities.md > invitationToken; functional-design-questions.md > Q2 | 招待の取消・再発行・訂正の手段が無く、Q2 で確定した運用前提と矛盾する。Q2=A の理由は「管理者が無効化して再招待することで実質的に失効させる」だが、(a) 状態遷移の注記は invited から disabled への遷移を想定せず `DELETE` を invited 対象で 404 とすることを推奨している(実装判断として先送り。`[assumption]`/`[open question]` の形式でも記録されていない)、(b) email は一意で BR4.1 は重複を 422 とするため、無効化済みユーザーの email での再招待もできない。結果として無期限の UUIDv4 トークンは、宛先の誤入力・メール誤送信・DB 漏えいの際に無効化できず、トークン保持者は当該ロールを持つアカウントのパスワードを設定できる(project.md の Mandated が求める認証情報保護の観点で高リスク)。 | 招待中ユーザーに対する取消(トークン無効化)と再招待(トークン再発行)のいずれかを BR/workflow として設計するか、招待中ユーザーの削除・email 訂正を許可する。BR4.1 の重複判定が disabled ユーザーを含むか否かを明記する。先送りする場合は open question として正式に記録し、Q2 の「無効化して再招待」という前提を訂正する。 | New |
| R-04 | Major | FD/functional-spec.md > Assumptions & Open Questions(C11 の項); FD/rules.md > BR4.6; FD/traceability.json > FR2.3; CS > C11, C10 `getGroupDerivedRoleIds` | user-management が提供元である C11 `UserAccountLookupApi`(`findByEmail` / `verifyPasswordHash` / `isDisabled` / `revokeRefreshTokensOnDisable`)の提供側の振る舞いを規定する workflow・rule が無い。例: invited(passwordHash が null)ユーザーへの `verifyPasswordHash`、disabled ユーザーの扱い、email の大文字小文字、定数時間比較、`UserAccount.roleIds` の内容。`revokeRefreshTokensOnDisable` の方向性は open question として正直に記録されているが、代替として位置付けた `UserChangedEvent` は entities.md で「fire-and-forget・監査用」と定義されており、FR2.3 の「即時失効」を担保する機構としては信頼性の保証が無い(project.md Correction も配信信頼性は NFR に委ねると述べているのみ)。それにもかかわらず traceability.json は FR2.3 を `OK` としている。加えて C10 は「user-management が User.roleIds(直接付与分)と `getGroupDerivedRoleIds`(Group 経由分)を合成して選択可能ロール一覧を構成する(W3)」と明記しているが、本設計の W3 と `UserAccount.roleIds` に Group 経由ロールの合成が無く、open question にもなっていない。 | C11 の各メソッドの提供側ルールを追加する。FR2.3 の即時失効(リフレッシュトークンの再認証拒否)を実現する経路を確定するか、`isDisabled` チェックを再認証時に必須とする等の代替を記述し、traceability の FR2.3 を実態に合わせる(部分カバー/GAP)。C10 が想定する Group 経由ロール合成の責務を本ユニットが持つのか、authentication-service が持つのかを確定して契約と整合させる。 | New |
| R-05 | Major | FD/entities.md > UserChangedEvent(operation, actor); FD/functional-spec.md > W2 手順6, Assumptions(最終項目); FD/rules.md > BR4.9; CS > C6 AuditLogEntry | audit-logging が購読するイベントの形状が確定していない。(a) W2 手順6 は「operation=INVITED、またはUPDATED」「1件か2件か」をコード生成へ先送りし、BR4.9 は「操作1回につき1件」、Assumptions は「2件(W1: INVITED、W2: UPDATED)を推奨」と3か所で表現が異なる。(b) `operation` の許容値は [INVITED, UPDATED, DISABLED] のみで、invited から active への遷移(パスワード設定)を表す値が無く、管理者による更新(UPDATED)と区別できない。afterValue は passwordHash を除外するためパスワード設定の事実も監査上判別できない。(c) `actor` は required だが、W2 は認証不要(C5 で `security: []`)で操作者が被招待者本人、W5 は操作者がシステムであり、いずれも値が定義されていない。(d) W5(初期管理者の作成)はイベントを発行しない。BR4.9 のトリガー一覧にも含まれず、operation にも作成を表す値が無い。(e) C6 の `AuditLogEntry` は `targetType` を持つが、UserChangedEvent には `targetType` が無い(audit-logging がイベント型から導出するのであれば、その前提を明記する必要がある)。 | W2 のイベントを1件/2件のどちらにするか、operation の値(例: ACCEPTED/ACTIVATED を追加)を機能設計として確定し、W2/BR4.9/entities.md を一致させる。W2 の actor と W5 の actor/イベント発行の有無を定義する。targetType の導出方法(イベント型か属性か)を明記する。 | New |
| R-06 | Major | FD/functional-spec.md > W5(手順2); FD/rules.md > BR4.7; FD/entities.md > UserPreference; FD/rules.md > BR4.3 | 初期管理者の自動作成(FR2.4)が設計として未完成。(a) 割り当てるロールが「初期管理者用の固定ロール」と書かれるのみで、どのロールか・いつ存在が保証されるか・BR4.5(roleId 実在検証)を適用するのかが不明。(b) 「practices-discovery のブートストラップ例外(BR3.13)」と記述しているが、BR3.13 は practices-discovery ではなく permission-engine のルール番号として参照されているうえ、渡された共有契約(C10 等)には BR3.13 も該当する例外も記載が無く、本設計の記述からは整合性を検証できない。(c) UserPreference は BR4.3 で招待受諾時にのみ作成されるため、初期管理者は UserPreference を持たず、entities.md の User と UserPreference の 1..1 関係に反し、`GET /api/me/preferences`(FR9.1)の未作成時の挙動が未定義である。(d) application.yml のパスワードに FR2.5 の8文字検証を適用するかが未定義。(e) 起動時に email 既存(disabled 状態を含む)の場合は何もしない旨のみで、disabled の初期管理者が復旧不能になる点に触れていない。 | 初期管理者のロールの決定方法と permission-engine 側の存在保証(ブートストラップ例外の参照先を permission-engine の該当ルールとして正確に記述)を明記する。W5 でも UserPreference を既定値で作成するか、GET の未作成時挙動(既定値返却等)を BR として定義する。パスワード検証と disabled 既存ユーザーの扱いを定義する。 | New |
| R-07 | Major | FD/functional-spec.md > W3, W1, W4; FD/traceability.json > FR2.2; CS > C5(`GET /api/users`、`PUT /api/users/{userId}`、`DELETE`) | C5 に対する完全性の欠落と、契約の黙示的な拡張がある。(a) C5 の `GET /api/users`(ユーザー一覧)に対する workflow・rule が無い(ページング、disabled/invited の表示、レスポンスから passwordHash と invitationToken を除外すること)。(b) C5 の `PUT /api/users/{userId}` は requestBody が未定義で、W3 は「name/roleIds等」とするのみのため、更新可能な項目(email・status を変更可能か)がコード生成者に確定できない。FR2.2 の traceability も BR4.5(roleId 検証)のみを指し、更新自体のルールが無い。(c) 本設計は 401・404(PUT/DELETE/accept)・422(PUT)や SMTP 送信失敗時の HTTP ステータス(未指定)を使うが、C5 は PUT/DELETE を 200/204/403 のみ、accept を 200/422 のみとしており、追補が必要な open question は theme/fontSize/locale の追加(Q4)の1件のみである。 | GET /api/users の workflow と出力項目(機微項目の除外)を追加する。PUT の更新可能項目と検証ルールを明記する。C5 に追加が必要なレスポンスコード・リクエストボディを、既存の open question(C5 追補)へ列挙して記録する。 | New |
| R-08 | Minor | FD/functional-spec.md > W4, 状態遷移; FD/rules.md > BR4.6 | 無効化の周辺仕様が不足している。管理者による自己無効化・最後の有効な管理者の無効化を防ぐ規則が無い(全管理者ロックアウトの可能性)。すでに disabled のユーザーへの `DELETE` を再実行した場合の冪等性・イベント重複が未定義。disabled からの復帰手段が無く、email が一意のまま残ることは R-03 とも関連する。 | 自己/最終管理者の無効化に対する扱い(禁止または許容の明示)と、disabled への再無効化時の挙動を rules に追記する。復帰手段を対象外とする場合は明記する。 | New |
| R-09 | Minor | FD/rules.md > BR4.2, BR4.4; FD/entities.md > invitationToken, passwordHash; FD/functional-spec.md > W2 | 認証情報まわりの実装上の曖昧さ。(a) 受諾後もトークン値を「保持してよい」とするのは不要で、受諾時に null 化すべき。(b) 同一トークンの並行受諾(二重送信)や、同時招待による email 重複の競合時に、アトミックな状態遷移/一意制約違反の 422 変換を行うかが未定義。(c) トークンは URL パスに載るためアクセスログ・Referer に残るが、BR4.4 の禁止対象はパスワードのみでトークンのログ抑止が無い。(d) Argon2id のパラメータ(メモリ・反復・並列度)、パスワードの最大長(長大入力によるハッシュ計算 DoS)、文字数の数え方(code point)が未定義。(e) 使用済みトークン(status が invited 以外)に対する応答が「404または422」と曖昧。 | トークンのライフサイクル(受諾後の null 化)、並行受諾の排他、ログへのトークン非出力、Argon2id パラメータと最大長の扱い(NFR 設計へ委ねる場合は明記)、使用済みトークンの応答コードを1つに確定する。 | New |
| R-10 | Minor | FD/functional-spec.md > エンティティ関連図; FD/entities.md > User.entity_constraints; FD/functional-spec.md 全体の mermaid | 内部整合性の軽微な不一致。ER 図は User と UserPreference を「必ず1対必ず1」の関係として描き、entities.md の関係も両側とも 1..1 としているが、招待中(および W5 の初期管理者)には UserPreference が存在しない設計と矛盾する(UserPreference 側は0または1になる)。entities.md の email に関する一意制約の記述(「emailの組で一意」)は「組」が不要で意味が不明瞭。2つの mermaid 図に、CLAUDE.md の Conventions が求めるテキストフォールバックが無い。 | ER 図とカーディナリティを実態(User に対して UserPreference が0または1、または W5/W2 で必ず作成)に合わせる。誤記を修正し、mermaid にテキストフォールバックを追加する。 | New |
| R-11 | Minor | FD/traceability.json > upstream_ids, coverage(FR2.8, FR2.2, FR2.3), reverse | (a) unit-of-work-story-map.md は U4 の担当に FR1.6(横断・一部)を含めているが upstream_ids に無い(BR は BR4.4 等が実質担うため理由を N/A で記録すべき)。(b) FR2.8 の target の注記は「W1の手順4」とするが、メール送信は W1 の手順6(手順4は roleIds 検証)。(c) FR2.8(SMTP 設定と Mailpit)自体を満たす BR が無く BR4.1 に相乗りしている。(d) 上記 R-04 のとおり FR2.3 の `OK` は過大。検証したところ、traceability.json の全 BRx.y は rules.md に存在し、FR2.1〜FR2.6・FR2.8・FR9.1・FR10.1 は upstream_ids でカバーされている。 | FR1.6 の扱いを追記し、注記の手順番号を修正する。FR2.8 に専用の BR(送信失敗時の挙動を含む)を割り当てるか、相乗りの根拠を明記する。FR2.3 の status を実態に合わせる。 | New |
| R-12 | Minor | FD/rules.md > BR4.1, BR4.3, BR4.10; FD/functional-spec.md > W1 手順3 | (a) email のフォーマット検証・正規化(ログイン識別子として C4 が email を使うため、大文字小文字違いの重複が別アカウントになり得る)・name の必須/長さ検証など、フィールド単位の入力検証ルールが未定義(project.md Mandated の「フィールド単位のエラー」に従う対象が不明)。(b) screenKey `"user-management"` は BR4.10 に直書きされているが、渡された契約(C10)には screenKey の値の定義が無い(C3 の `"config-import-export"` のような予約の記載が無い)。(c) roleExists による実在検証は登録時点のみで、後に permission-engine 側でロールが削除された場合の参照不整合が扱われていない。 | email の検証・正規化ルールを追加する。screenKey の値を permission-engine/menu-navigation 側と整合させて契約(または open question)に記録する。ロール削除時の User.roleIds の扱いを permission-engine の設計で確認する旨を open question に追加する。 | New |

### Validation Tool Results

| Tool | Result | Interpretation |
|---|---|---|
| traceability(手動確認、advisory) | traceability.json の target に挙がる BR4.1〜BR4.10 はすべて rules.md に存在。upstream_ids(FR2.1〜FR2.6, FR2.8, FR9.1, FR10.1)は担当 FR を網羅。FR1.6(U4 一部)は未記載 | 構造上は PASS。FR2.3 の OK の実質性・FR2.8 の注記の手順番号・FR1.6 は R-04/R-11 のとおり |
| 内部整合(entities.md YAML / rules.md YAML / functional-spec.md / traceability.json) | BR4.9 の W2 発行方針が3か所で不一致、ER 図とカーディナリティの不一致を検出 | R-05、R-10 |
| 契約整合(CS: C5/C10/C11/C14, components.md, unit-of-work.md) | C5 の応答コード・PUT 本文・GET workflow の不足、C11 提供側の未設計、C14 の consumers に user-management が無い点を検出。BR3.13 は渡された共有契約から検証不能(read-scope の範囲外のため sibling は開いていない) | R-02、R-04、R-06、R-07 |

### Summary

C5/C10/C11 に対する本ユニットの追補(roleExists、theme/fontSize/locale)は open question として正直に記録されている一方、(1) ロール付与による権限昇格(project.md Forbidden)に対するルールの欠落、(2) 認可に必要な activeRoleId の取得経路と U4 と U5 の循環依存リスク、(3) 招待の取消・再発行手段が無く Q2 の運用前提と矛盾すること、(4) C11 提供側と C10 の Group 経由ロール合成の未設計、(5) 監査イベント形状と初期管理者ブートストラップの未確定、(6) C5 の workflow・応答コードの欠落、という6件の Major があり、コード生成者が設計者への確認なしに実装できる状態ではないため NOT-READY とする。
