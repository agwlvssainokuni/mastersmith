# Requirements Analysis — Questions

## Q1. 性能・スケールのNFR目標値をどう設定しますか?

想定利用規模は数十名程度(`feasibility-assessment.md`)ですが、応答時間や同時アクセス数、1テーブルあたりの想定行数といった数値目標がまだ確定していません。非機能要件は数値化できて初めて検証可能になります。

- A. 一覧・詳細画面とも応答時間3秒以内(95パーセンタイル)、同時アクセスは最大50ユーザー、1テーブルあたり最大10万行程度を想定する
- B. より厳しい目標(例: 応答時間1秒以内)を設定する
- C. 明確な数値目標は設定せず、実用上問題ない範囲でよいとする
- D. 今は確定せず、後続の設計ステージ(NFR Requirements)で改めて確定する
- X. Other (please specify)

[Answer]: A. 一覧・詳細画面とも応答時間3秒以内(95パーセンタイル)、同時アクセスは最大50ユーザー、1テーブルあたり最大10万行程度を想定する

## Q2. OTELエクスポート対応として、最低限どの可観測性情報を出力できればよいですか?

実環境デプロイは今回のワークフロー対象外ですが、アプリ自体はメトリクス・トレース・ログをOTEL基盤へexportできる実装が必要です(`intent-statement.md`)。最低限満たすべき項目を確認したいです。

- A. HTTPリクエストのレイテンシ・エラー率、アプリのヘルスチェック、構造化ログ(JSON形式、リクエストID付与)を出力できればよい
- B. 上記に加え、DBクエリのトレースも必須とする
- C. 具体的な項目は今は決めず、設計ステージ(NFR Design)で検討する
- X. Other (please specify)

[Answer]: A. HTTPリクエストのレイテンシ・エラー率、アプリのヘルスチェック、構造化ログ(JSON形式、リクエストID付与)を出力できればよい

## Q3. データ更新時の同時編集の競合をどう検出・処理しますか?

`user-flow.md`には「保存時の競合・DBエラー→エラーメッセージ表示、詳細・編集画面に留まる」とありますが、具体的な競合検出方式(楽観ロック等)がまだ決まっていません。

- A. 楽観ロック(更新日時やバージョン列で検出)を採用し、競合時はエラーメッセージを表示して保存をブロックする
- B. 悲観ロック(編集中はレコードをロックし、他ユーザーの編集を制限する)を採用する
- C. 競合検出は行わず、最後に保存した内容で上書きする(Last-Write-Wins)
- D. 今は決めず、後続の設計ステージで確定する
- X. Other (please specify)

[Answer]: A. 楽観ロック(更新日時やバージョン列で検出)を採用し、競合時はエラーメッセージを表示して保存をブロックする

## Q4. セッションのタイムアウト・同時ログインポリシーはどうしますか?

認証後のセッション管理(アイドルタイムアウトの有無・時間、同一ユーザーの複数デバイス同時ログインを許可するか)がまだ決まっていません。

- A. アイドルタイムアウト30分。複数デバイスからの同時ログインを許可する
- B. アイドルタイムアウト30分。ただし同時ログインは1セッションのみ許可し、新規ログインで旧セッションを無効化する
- C. アイドルタイムアウトの時間は管理画面から設定可能にする(固定値にしない)
- D. 今は決めず、後続の設計ステージで確定する
- X. Other (please specify)

[Answer]: A. アイドルタイムアウト30分。複数デバイスからの同時ログインを許可する

## Q5. ログイン失敗時のアカウントロックアウトは実装しますか?

ブルートフォース攻撃対策として、連続ログイン失敗時にアカウントを一時ロックする機能の要否と閾値がまだ決まっていません。

- A. 5回連続失敗で15分間ロックする
- B. ロックアウトは実装せず、失敗ログの記録のみ行う
- C. 具体的な閾値(失敗回数・ロック時間)は管理画面から設定可能にする
- X. Other (please specify)

[Answer]: C. 具体的な閾値(失敗回数・ロック時間)は管理画面から設定可能にする

## Q6. パスワードの複雑性要件はどう設定しますか?

パスワード等の認証情報をハッシュ化して保存することは既に確定していますが(`project.md`)、パスワード自体の複雑性要件(最小文字数・文字種)はまだ決まっていません。

- A. 最小8文字、英大文字・小文字・数字を含むことを必須とする
- B. 最小12文字、記号も含むことを必須とする、より厳格な要件とする
- C. 複雑性要件は特に設けず、最小文字数(8文字)のみを要件とする
- X. Other (please specify)

[Answer]: C. 複雑性要件は特に設けず、最小文字数(8文字)のみを要件とする

## Q7. 多言語対応(i18n)のMVPで用意する対象言語はどこまでにしますか?

`scope-document.md`では「表示名・バリデーションメッセージのi18nキー化」がScopeに含まれていますが、実際にMVPで翻訳リソースを用意する言語がまだ決まっていません。

- A. 日本語のみ。i18nキー化の仕組みは実装するが、MVPでは日本語の翻訳リソースのみ用意し、他言語は将来追加できる構造にする
- B. 日本語・英語の2言語分の翻訳リソースを用意する
- C. 具体的な言語は未定。i18nキー化の仕組みだけ実装し、翻訳リソースの追加自体は今回のスコープ外とする
- X. Other (please specify)

[Answer]: B. 日本語・英語の2言語分の翻訳リソースを用意する

## Q8. 設定ファイルのexport/importの対象範囲とフォーマットはどうしますか?

`scope-document.md`で「設定ファイルのexport/importにも対応する」とありますが、対象範囲(スキーマ定義のみか、メニュー構成・RBAC設定も含むか)とファイル形式がまだ決まっていません。

- A. スキーマ定義(テーブル・カラムの表示設定等)、メニュー構成、RBAC設定をすべて含め、JSON形式でexport/importする
- B. スキーマ定義とメニュー構成のみを対象とし、RBAC設定(権限)は対象外とする(権限は導入環境ごとに個別設定する前提)
- C. スキーマ定義のみを対象とする
- X. Other (please specify)

[Answer]: A. スキーマ定義(テーブル・カラムの表示設定等)、メニュー構成、RBAC設定をすべて含め、JSON形式でexport/importする

## Q9. マスタデータ本体(業務データの行データ)のCSV等によるexport/importは必要ですか?

Q8の設定ファイルexport/importとは別に、一覧・詳細編集で扱う業務データ本体(行データ)を一括でエクスポート・インポートする機能が必要かがまだ決まっていません。

- A. 業務データ本体のエクスポート・インポートは今回のスコープに含めない(画面からの個別CRUDのみ)
- B. 一覧のCSVエクスポートのみ含める(インポートは対象外)
- C. エクスポート・インポートの両方を含める
- X. Other (please specify)

[Answer]: C. エクスポート・インポートの両方を含める

## Q10. 一覧画面のデフォルトページサイズ・ソート順はどうしますか?

`wireframes.md`に一覧画面のページングは記載がありますが、具体的なデフォルト表示件数・初期ソート順がまだ決まっていません。

- A. デフォルト20件/ページ、初期ソートは主キー昇順
- B. デフォルト50件/ページ、初期ソート順は設定(業務固有の設定層)で指定可能にする
- C. ページサイズ自体をユーザーが画面上で選択可能にする(10/20/50/100件)
- X. Other (please specify)

[Answer]: C. ページサイズ自体をユーザーが画面上で選択可能にする(10/20/50/100件)

## Q11. 監査ログの保持期間はどうしますか?

監査ログを改ざん・削除できない追記専用とすることは既に確定していますが(`project.md`)、保持期間(無期限か、一定期間後にアーカイブするか)はまだ決まっていません。

- A. 無期限に保持する。今回のスコープでは削除・アーカイブ機能は実装しない
- B. 保持期間を設定可能にし、期間経過後は別のアーカイブ先へ移動する機能を用意する
- C. 今は決めず、後続の設計ステージで確定する
- X. Other (please specify)

[Answer]: A. 無期限に保持する。今回のスコープでは削除・アーカイブ機能は実装しない

## Q12. 初期管理者アカウントはどのように作成しますか?

`scope-document.md`に「初期管理者登録」とありますが、具体的な作成手順(アプリ初回起動時のセットアップ画面、設定ファイル/環境変数による自動投入など)がまだ決まっていません。

- A. アプリ初回起動時、管理者が1人も存在しない場合に限り、セットアップ画面で初期管理者を登録できるようにする
- B. 起動時の設定ファイル(または環境変数)に初期管理者情報を記載し、アプリ起動時に自動投入する
- C. 両方に対応する(セットアップ画面での登録・設定ファイルでの自動投入のどちらも可能にする)
- X. Other (please specify)

[Answer]: X. Other — `application.yml`(設定ファイル)に初期管理者のメールアドレス・パスワードを設定し、アプリ起動時にその管理者アカウントがまだ存在しなければ自動作成する(既に存在する場合は何もしない)。セットアップ画面での登録は行わない。

## Q13. ユーザーを無効化した際、既存のログイン中セッションはどう扱いますか?

ユーザ管理の「無効化」機能について、無効化した瞬間に対象ユーザーの既存ログインセッションを即座に無効化するかがまだ決まっていません。

- A. 無効化と同時に、該当ユーザーの既存セッションを即座に無効化する(次のリクエストでログイン画面に戻す)
- B. 既存セッションはそのままセッション有効期限まで維持し、次回ログイン時のみブロックする
- X. Other (please specify)

[Answer]: X. Other — アクセストークンは既存の有効期限までそのまま利用を許可するが、リフレッシュトークンは無効化と同時に即時失効させる(アクセストークン失効後の再認証(トークン更新)はできなくなる)。

## Q14. ユーザー招待メールの送信方式はどうしますか?

`scope-document.md`の「招待メールでパスワード・氏名設定」について、メール送信インフラ(SMTP等)の扱いがまだ決まっていません。実環境デプロイ・環境構築は今回のワークフロー対象外です。

- A. SMTP設定(ホスト/認証情報等)を管理画面から設定可能にし、アプリからメール送信する機能を実装する。開発・動作確認用には、OTEL確認用ローカルコンテナ環境と同様に、ローカルでメール受信を確認できる環境(MailHog等)も用意する
- B. メール送信機能自体はアプリに実装するが、実際のSMTPサーバーとの接続確認(実機テスト)は今回のスコープ外とする
- C. 招待メール送信機能は見送り、初期パスワードを管理画面上に表示する方式に変更する
- X. Other (please specify)

[Answer]: A. SMTP設定(ホスト/認証情報等)を実装する。ただし設定箇所は管理画面ではなく `application.yml`(設定ファイル)とする(Q15の回答で修正)。開発・動作確認用のローカルメール受信環境はMailpitを使用する(MailHogではなくMailpit)。

## Q15(フォローアップ). アクセストークン・リフレッシュトークンの有効期限をどう設定しますか?

Q13の回答で「アクセストークンは有効期限まで許可、リフレッシュトークンを即時無効化」とのことでしたが、これはJWTベースの認証(アクセストークン+リフレッシュトークン方式)を前提としていると理解しました。一方Q4では「アイドルタイムアウト30分」と回答いただいており、これがどちらのトークンの有効期限に対応するのかを明確にしたいです。

- A. アクセストークンの有効期限は短め(例: 15分)とし、リフレッシュトークンの有効期限を実質的な「アイドルタイムアウト30分」として扱う(30分間APIアクセスがないとリフレッシュトークンも失効する)
- B. アクセストークンの有効期限をそのまま30分とし、リフレッシュトークンはより長い期間(例: 7日間)有効とする
- C. 具体的なトークン有効期限の数値は今は決めず、後続の設計ステージ(機能設計・NFR設計)で確定する
- X. Other (please specify)

[Answer]: X. Other — アクセストークンの有効期限を10分、リフレッシュトークンの有効期限を30分とする(Q4の「アイドルタイムアウト30分」はリフレッシュトークンの有効期限に対応)。両トークンの有効期限は `application.yml`(設定ファイル)で設定可能にする。

## Consolidated Summary Confirmation

- 性能NFR: 応答時間3秒以内(95パーセンタイル)、同時アクセス最大50ユーザー、1テーブル最大10万行を想定
- OTEL可観測性: HTTPリクエストのレイテンシ・エラー率、ヘルスチェック、構造化ログ(JSON、リクエストID付与)を最低限出力
- データ更新競合: 楽観ロック(更新日時/バージョン列)で検出し、競合時はエラー表示して保存をブロック
- 認証・セッション: アクセストークン有効期限10分、リフレッシュトークン有効期限30分(Q4の「アイドルタイムアウト30分」に対応)。複数デバイスからの同時ログインを許可。両トークンの有効期限は`application.yml`で設定可能
- ログイン失敗ロックアウト: 実装するが、失敗回数・ロック時間は管理画面から設定可能な閾値とする
- パスワード: 複雑性要件は設けず、最小8文字のみ
- 多言語対応: 日本語・英語の2言語分の翻訳リソースを用意
- 設定ファイルexport/import: スキーマ定義・メニュー構成・RBAC設定をすべて含め、JSON形式で対応
- マスタデータ本体のexport/import: CSV等によるエクスポート・インポートの両方に対応
- 一覧画面のページサイズ: ユーザーが画面上で選択可能(10/20/50/100件)
- 監査ログ保持期間: 無期限に保持し、削除・アーカイブ機能は実装しない
- 初期管理者アカウント: `application.yml`にメールアドレス・パスワードを設定し、アプリ起動時に当該アカウントが存在しなければ自動作成する(セットアップ画面は用意しない)
- ユーザー無効化時のセッション扱い: アクセストークンは有効期限まで利用可能、リフレッシュトークンは即時失効(再認証不可に)
- ユーザー招待メール: SMTP接続情報を`application.yml`で設定し、アプリから送信する機能を実装。開発・動作確認用のローカルメール受信環境はMailpitを使用

Does this all look correct before I generate the requirements artifact?

- Looks correct
- Request changes

[Answer]: Looks correct
