# 発見されたルール(ドラフト)

> **ステータス: ドラフト(未承認)**
> このファイルは Mandated(必須)/ Forbidden(禁止)の候補を記録する場所である。
> `## Mandated`/`## Forbidden` に計上できるのは、人間が明示的に述べた強い制約のみである。
> 本ドラフト作成時点ではインタビューステップ(人間への質問)を未実施であるため、
> 以下は基本的に空欄・プレースホルダーである。インタビュー実施後、人間が明示した
> ハード制約のみをここに追記し、最終的に `aidlc-state.ts practices-promote` 等の
> 決定論的な書き込み経路を通じて `team.md` / `project.md` の該当セクションへ昇格される。

## Mandated

<!-- ALWAYS [挙動] の形式で記載する。人間が明示的に述べたハード制約のみを計上する。 -->

(現時点ではなし。インタビューステップで人間から明示的なALWAYS制約が述べられた場合に追記する。)

## Forbidden

<!-- NEVER [挙動] の形式で記載する。人間が明示的に述べたハード制約のみを計上する。 -->

(現時点ではなし。インタビューステップで人間から明示的なNEVER制約が述べられた場合に追記する。)

## 補足

- `project.md` の `## Forbidden` / `## Mandated` は現時点で空欄であり、本ステージ以前に
  確定した恒久的な必須/禁止事項は存在しない。
- `project.md` の `## Corrections` にある「review-brief summary失敗時の代替手順」のような
  学習済み項目は、承認・引き継ぎ(approval-handoff)ステージで既に記録済みの是正事項であり、
  本ステージの新規発見対象ではない。
- 本ステージのインタビューでは、チームプラクティス(team-practices.md)の確認と併せて、
  「絶対にやってはいけないこと」「必ず行うべきこと」が他にないかを人間に問い、
  回答があれば本ファイルへ反映する。
