# Scalability Design: config-management

## スケーリングアーキテクチャ

単一インスタンス構成(NFR2.1)。水平スケーリングは設計しない。

## キャッシュサイズの想定

Caffeineキャッシュ(BR6.1)は、想定運用規模(1業務あたり数十〜数百テーブル程度)ではメモリ使用量が問題にならない(NFR2.2)。将来的にテーブル数が大幅に増加した場合は、Caffeineの最大エントリ数設定(maximumSize)によるキャッシュ制御を検討する余地があることを記録しておく(本ステージのスコープ外)。
