# Performance Design: frontend-admin

## 応答速度の一般方針

requirements.md NFR1(厳密な数値目標なし)を踏襲する。

## 大量データ取得の回避

監査ログ画面・アカウント一覧・テーブル一覧の各画面コンポーネントは、バックエンドのページネーション(page/size)をそのまま利用し、フロントエンド側で全件取得後にクライアント側ページングする実装は行わない(NFR1.2)。APIクライアントはpage/sizeパラメータをそのままバックエンドへ転送する。
